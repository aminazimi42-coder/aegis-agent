T17 — Twin speech policy and budget stop. Do ONLY this directive. Do not start T18. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 05b6613. Do not rewrite README. Do not add payments, OAuth, or desktop UI. Do not call a live paid network. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T16. HEAD 05b6613. pytest was 196 green after T16.

GOAL: the model must not claim an external action happened. If the token budget is exhausted, complete_safe must not call the provider.

Implement in core/llm_safety.py:

- FORBIDDEN_CLAIMS: tuple of lowercase phrases: "transferred funds", "pushed to origin", "payment sent", "email sent"
- After provider returns, if ok and any forbidden phrase is in text.lower(): set ok=False and text="rejected: external action claim"
- Before get_provider().complete, if env AEGIS_LLM_BUDGET_EXHAUSTED is "1": do not call complete; return ok=False, text="rejected: budget exhausted", tokens 0, tool=""
- Keep redact_secrets and the T15 tool cage. Keep ledger best-effort.

tests/test_t17_llm_policy.py
- Monkeypatch provider to return "payment sent to vendor" -> ok False and rejected in text
- With AEGIS_LLM_BUDGET_EXHAUSTED=1, complete_safe does not need a live provider; ok False and "budget" in text.lower()
- T15 wire_transfer still rejected
- AEGIS_DATA_DIR temp; no live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "import os; os.environ['AEGIS_LLM_BUDGET_EXHAUSTED']='1'; from core.llm_safety import complete_safe; r=complete_safe('hi', tenant_id='t17'); print(r['ok'] is False, 'budget' in r['text'].lower())"
   Required: True True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 196
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t17): reject external-action claims and stop on exhausted budget"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
