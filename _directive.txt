T139_FIX2 — Export button is disabled in served HTML so click never runs. Enable it. Do ONLY this directive. Do not start T140. Do not read task_instruction.txt. Do not rewrite LICENSE or README Author. English only in repo files.

Laptop proof 2026-09-10 04:13 CEST after cac45a7 and uvicorn PID 41916:
curl -s http://127.0.0.1:8741/ | grep shows:
  <button id="btn-signed-brief" onclick="exportSignedBrief()" disabled>Export signed brief</button>
The function exportSignedBrief exists and posts /api/v1/twin/brief/signed-export and sets Exporting...
Click still produces no POST because the control is disabled. That is the only remaining defect.

GOAL: The served operator page must ship #btn-signed-brief without a disabled attribute, and no script may set that button disabled when the engine is up.

Implement:

A) Markup
- In the operator HTML that GET / actually serves, the signed-brief button must be:
  <button id="btn-signed-brief" type="button" onclick="exportSignedBrief()">Export signed brief</button>
- Remove the disabled attribute from that tag.

B) Script
- Any line that sets btn-signed-brief.disabled = true / bw.disabled = true when the label is Export signed brief must stop doing that while engineOnline is true.
- Other buttons may keep their own guards. Do not disable the export button behind profilePrefill.

C) tests/test_t139_signed_export_button.py
- Add test_signed_export_button_markup_is_not_disabled: the HTML string served by GET / (TestClient) contains id="btn-signed-brief" and that same tag does not contain disabled.
- Keep existing tests.

D) README
- Do not claim the laptop click worked. One Now sentence: export button is enabled when the local engine is up.

Verification, exact order. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from fastapi.testclient import TestClient; from app.server import create_app; h=TestClient(create_app()).get('/').text; i=h.find('btn-signed-brief'); chunk=h[i:i+180]; print(chunk); assert 'btn-signed-brief' in h and 'disabled' not in chunk"
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "fix(t139): enable signed-export button; do not ship disabled"
7. git push origin main
8. Print new SHA, pytest count, and the proof chunk.

Stop after push. Do nothing else.
