T39 — Persist approvals and FinOps budgets in SQLite. Do ONLY this directive. Do not start T40. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend a6a0815. Do not rewrite README. Do not add a payment gateway. Source, comments, commit message: English only.

Baseline on main: T01–T38. HEAD a6a0815. pytest was 336 green after T38.

GOAL: process restart must not wipe approval rows or tenant budget remaining. Same public routes.

Implement:

1) core/twin_persist.py
- db path = Path(AEGIS_DATA_DIR or "data") / "aegis.sqlite"
- init_schema() tables:
  approvals(id TEXT PK, tenant_id TEXT, title TEXT, status TEXT, payload TEXT, created_at TEXT, decided_at TEXT)
  budgets(tenant_id TEXT PK, cap REAL, spent REAL, updated_at TEXT)
- put_approval / get_approval / list_approvals
- get_budget / set_budget / add_spend(tenant_id, amount) -> remaining
- All functions open their own connection; close after use

2) Wire without breaking callers
- app/server.py approvals/request and approvals/{id}/decide must use put_approval/get_approval instead of only app.state.approvals
- core/finops_autopilot.py: if a remaining/cap method exists, persist via set_budget/add_spend; if the class is in-memory only, add load/save hooks on those methods without changing method names callers already use
- Keep 400 ValueError shape via existing twin_value_error_response where twin routes already use it

3) tests/test_t39_persist_restart.py
- AEGIS_DATA_DIR temp
- put_approval then new Python process reading same db file still finds the row
- add_spend then new function call on a fresh object/connection sees spent
- FastAPI POST /approvals/request then GET-or-decide still works after creating a second TestClient (simulates restart of app.state)
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_persist import put_approval, get_budget; print(callable(put_approval))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 336
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t39): persist approvals and finops budgets in sqlite"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
