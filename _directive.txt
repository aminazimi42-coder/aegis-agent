T32 — Principal one-pager / resume pack. Do ONLY this directive. Do not start T33. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 62d0981. Do not rewrite README. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T31. HEAD 62d0981. pytest was 294 green after T31.

GOAL: one page from the consented profile plus names of existing work products. No live LLM.

Implement:

1) core/twin_resume_pack.py
- render_resume(tenant_id) -> dict {tenant_id, path}
- Require consented profile or raise ValueError("no consented profile")
- Load profile via the same helper other twin modules use
- Sections: Name/role from profile fields that exist (skip missing), Work products = filenames under work_products/{tenant_id}/, Decisions count if list_decisions works
- Write work_products/{tenant_id}/resume.md
- Overwrite on each call

2) API POST /api/v1/twin/resume/render body {tenant_id}
3) CLI resume --tenant ID

4) tests/test_t32_twin_resume_pack.py
- AEGIS_DATA_DIR temp
- without profile ValueError
- after T03 commit file exists and contains the tenant id or a profile name token
- FastAPI 200
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_resume_pack import render_resume; print(callable(render_resume))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 294
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t32): principal one-pager from profile and work products"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
