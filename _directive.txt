T40 — Durable token spend on every safe completion. Do ONLY this directive. Do not start T41. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 3d58724. Do not rewrite README. Do not call a live paid API in tests. Source, comments, commit message: English only.

Baseline on main: T01–T39. HEAD 3d58724. pytest was 342 green after T39.

GOAL: Echo stays default. complete_safe already uses get_provider. After each complete_safe, persist token spend so restart keeps the burn.

Implement:

1) core/llm_safety.py complete_safe
- After a successful provider.complete, call core.twin_persist.add_spend(tenant_id, total_tokens)
- tenant_id from kwargs if present else "default"
- If add_spend raises, still return the completion (do not hide the text)
- Do not change EchoProvider default
- Do not require AEGIS_LLM_BASE_URL in tests

2) core/twin_persist.py
- add_spend already exists from T39; if missing, implement it
- Optional table llm_calls is allowed but not required

3) tests/test_t40_llm_spend.py
- AEGIS_DATA_DIR temp
- get_provider() is EchoProvider when AEGIS_LLM_PROVIDER unset
- complete_safe("hello", tenant_id="t40") then get_budget("t40") spent > 0
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.llm_provider import get_provider; print(type(get_provider()).__name__)"
   Required: EchoProvider
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 342
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t40): persist token spend after complete_safe"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
