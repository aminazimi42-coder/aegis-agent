T80 — Cloud quota ledger stays outside twin actions. Do ONLY this directive. Do not start T81. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 3f135b8. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, a card form, or a license server. Do not un-quarantine desktop_engine.py. Do not weaken T56–T79. English only.

Baseline on main: T01–T79. HEAD 3f135b8. pytest was green after T79.

GOAL: each tenant has a local quota row that is not stored on twin_actions.

Implement:

1) core/twin_quota.py
- get_quota(tenant_id: str) -> dict with keys remaining and period_end
- set_quota(tenant_id: str, remaining: int, period_end: str) -> dict
- Default remaining is 0
- Store in AEGIS_DATA_DIR, not in twin_actions rows
- No card number, no payment id, no customer id

2) tests/test_t80_quota_store.py
- AEGIS_DATA_DIR temp
- test_default_remaining_is_zero
- test_set_and_get_quota
- test_quota_not_on_action_row
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_quota import get_quota, set_quota; print(callable(get_quota) and callable(set_quota))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t80): tenant quota ledger separate from actions"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
