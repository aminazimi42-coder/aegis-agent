T57 CONTINUE — finish tenant binding only. Do not start T58. Do not read task_instruction.txt. Do not patch SKILL.md. Do not force-push or amend cca81f9. Do not rewrite LICENSE or README. Do not revert T56 digest binding. English only.

Remote main is still cca81f9 (T56). T57 never pushed. Local disk may already contain a partial T57 edit (+26/-28). Use that work. Do not start from a blank file if the T57 changes are already present.

GOAL unchanged: action_id is not enough. Caller tenant_id must equal action.tenant_id before execute, reject, or outbox write.

Required end state:
1) execute(action_id, tenant_id) and reject(action_id, tenant_id) refuse on tenant mismatch
2) send_approved(tenant_id, action_id) refuses before writing .eml on tenant mismatch
3) server + twin_cli require tenant_id
4) tests/test_t57_tenant_binding.py with the four named cases
5) older execute(action_id) call sites updated
6) T56 digest checks still work

First commands:
- git status
- git log -1 --oneline
- git diff --stat

If HEAD is not cca81f9 and you did not commit T57, stop and print the SHA.
If T57 files are already edited, finish them. Do not delete them to rewrite from scratch unless they are broken.

Then run verification in order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_actions import execute; import inspect; print('tenant_id' in inspect.signature(execute).parameters)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 442
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t57): bind tenant to approve execute and outbox"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
