T34 — Local audio sidecar to a proposed task. Do ONLY this directive. Do not start T35. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend cea3de5. Do not rewrite README. Do not call cloud speech-to-text or whisper. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T33. HEAD cea3de5. pytest was 307 green after T33.

GOAL: if the principal drops note.wav plus note.txt next to it, treat the txt as the transcript and reuse T30. No audio decode.

Implement:

1) core/twin_audio_task.py
- from_audio(tenant_id, audio_path) -> dict from from_transcript
- Require consented profile or raise ValueError("no consented profile")
- Require Path(audio_path).is_file() or raise ValueError("audio not found")
- sidecar = same stem + ".txt"; if missing raise ValueError("transcript sidecar not found")
- Call core.twin_transcript_task.from_transcript(tenant_id, sidecar)
- Also write work_products/{tenant_id}/audio_task.md pointing at the sidecar name

2) API POST /api/v1/twin/audio/task body {tenant_id, audio_path}
3) CLI audio-task --tenant ID --file PATH

4) tests/test_t34_twin_audio_task.py
- AEGIS_DATA_DIR temp
- without profile ValueError
- after T03 commit write dummy.wav bytes and dummy.txt "Call the Berlin hotel tonight": result title or markdown contains Berlin hotel
- missing sidecar raises
- FastAPI 200
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_audio_task import from_audio; print(callable(from_audio))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 307
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t34): local audio sidecar transcript to proposed task"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
