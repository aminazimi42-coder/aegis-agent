T61 — Forget all active stores and write a deletion receipt. Do ONLY this directive. Do not start T62. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 4d548a0. Do not rewrite LICENSE or README. Do not weaken T56–T60. English only.

Baseline on main: T01–T60. HEAD 4d548a0. pytest was 458 green after T60.

GOAL: one tenant call clears that tenant's active twin stores and leaves a local receipt. Neighbor tenants stay intact.

Implement:

1) core/twin_memory_control.py
- Add forget_all(tenant_id: str) -> dict
- Clear forgotten+visible memory listing for that tenant
- Delete or empty that tenant's twin_actions rows
- Do not delete another tenant's rows
- Write work_products/{tenant_id}/deletion_receipt.md with tenant_id and UTC time
- Return {tenant_id, receipt_path, cleared: True}

2) tests/test_t61_forget_all.py
- AEGIS_DATA_DIR temp, two tenants
- test_forget_all_clears_actions_and_writes_receipt
- test_neighbor_tenant_survives
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_memory_control import forget_all; print(callable(forget_all))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 458
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t61): forget all tenant stores with deletion receipt"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
