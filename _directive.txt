T69 — Local SQLite backup and restore. Do ONLY this directive. Do not start T70. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 79ec6e8. Do not rewrite LICENSE or README Author. Do not weaken T56–T68. English only.

Baseline on main: T01–T68. HEAD 79ec6e8. pytest was 490 green after T68.

GOAL: one tenant can snapshot profile, actions, jobs, and receipts to a local file and restore them. Neighbor tenants stay intact. No network.

Implement:

1) core/twin_backup.py
- backup_tenant(tenant_id: str, dest: Path) -> Path
- restore_tenant(tenant_id: str, src: Path) -> dict
- Snapshot includes profile rows, twin_actions rows, jobs rows, and receipt files under work_products/{tenant_id}/receipts when present
- Restore overwrites only that tenant
- dest is a .sqlite or .zip under AEGIS_DATA_DIR; pick one and keep it local

2) tests/test_t69_backup_restore.py
- AEGIS_DATA_DIR temp, two tenants
- test_backup_then_restore_roundtrip
- test_neighbor_untouched
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_backup import backup_tenant, restore_tenant; print(callable(backup_tenant) and callable(restore_tenant))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 490
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t69): local tenant sqlite backup restore"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
