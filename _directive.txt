T163 — Single-instance lock + Status HTML duration and token cost. This is slice 1 of 4. Do ONLY this directive. Do not start T164. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not strip CTO in this slice. Do not rebuild the Mac pack. Do not add Stripe, a card form, a live license host, Grok bots, voice, Windows, WebSockets, Playwright, OS cron, or a seventh agent. Do not bind 0.0.0.0. Do not weaken T56–T162. English only in repo files. Do not copy $HOME/.aegis.

Baseline: T162 live ceea3bbf301ad187a0c994146c2619114597067f. pytest 891 passed 1 skipped.

GOAL: A second start_operator.sh on the same port must fail with a typed English error. The Home/Status panel on the operator page must show duration_ms and http_token_cost from the existing status JSON.

Implement both. Do not split. Commit and push in this same session before you stop.

1) Single-instance lock
- When the engine binds 127.0.0.1:8741, write a lock file under AEGIS_DATA_DIR that records pid and port.
- A second process that finds that port already bound must exit non-zero and print typed English: port 8741 already in use.
- Do not kill the first process. Do not bind another port. Stale lock from a dead pid may be replaced.

2) Status HTML
- The Home/Status panel that today shows the health JSON must also show duration_ms and http_token_cost.
- Echo keeps http_token_cost at 0. Do not invent billing.

3) tests/test_t163_single_instance_status_html.py
- test_second_bind_on_same_port_fails_typed
- test_stale_lock_from_dead_pid_is_replaced
- test_status_html_or_json_includes_duration_ms
- test_status_html_or_json_includes_http_token_cost
- test_echo_http_token_cost_is_zero
- Do not start a second live uvicorn in CI if a socket fixture can prove the lock. Use tmp_path. Do not write the developer's real $HOME/.aegis.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. If the tool budget is low after pytest, skip commentary and do git commit + push immediately.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('tests/test_t163_single_instance_status_html.py'); html=Path('app/static/index.html') if Path('app/static/index.html').exists() else Path('app.html'); print(t.is_file(), html.exists())"
   Required: True True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t163): single-instance lock and status duration on page"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

Stop only after a successful push. Do nothing else.
