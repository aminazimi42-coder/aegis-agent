T71 — Execute once per action_id. Do ONLY this directive. Do not start T72. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 87992b4. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, desktop UI, or a license server. Do not weaken T56–T70. English only.

Baseline on main: T01–T70. HEAD 87992b4. pytest was 501 green after T70.

GOAL: a second execute() on the same action_id does not rewrite the receipt or change status. First successful execute remains the only truth.

Implement:

1) core/twin_actions.py execute()
- If a receipt for this action_id already exists, return the existing row and do not write a second receipt
- If status is already executed, return that row
- Do not mark executed when the first receipt write failed (T59 stays)

2) tests/test_t71_execute_once.py
- AEGIS_DATA_DIR temp
- test_second_execute_does_not_rewrite_receipt
- test_second_execute_keeps_first_status
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_actions import execute; print(callable(execute))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 501
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t71): execute once per action_id"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
