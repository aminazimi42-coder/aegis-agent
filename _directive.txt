T98 — Readable queue, execute receipt, honest STATUS and health. Do ONLY this directive. Do not start T99. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend b5897ba. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not add Electron. Do not weaken T56–T97. English only.

Baseline on main: T01–T97. HEAD b5897ba. pytest was 596 green after T97.

GOAL: operator can read the queue and a receipt without guessing. Docs name quarantined leftovers.

Implement:

1) core/twin_local.py status
- pending lines show action_id, title, age, digest, specialist if present
- keep pending_digest

2) execute success prints a receipt line: action_id + digest + executed
- dry-run still writes nothing

3) STATUS.md
- one Not-shipped line: desktop_engine.py and .market are quarantined leftovers, not product

4) FastAPI
- GET /health returns JSON ok=true and does not claim SaaS production
- do not add auth or Postgres

5) tests/test_t98_queue_receipt_health.py
- test_status_lists_title_and_digest
- test_execute_prints_receipt
- test_health_ok
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; print(Path('tests/test_t98_queue_receipt_health.py').is_file())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 596
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t98): readable queue receipt health and quarantine note"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
