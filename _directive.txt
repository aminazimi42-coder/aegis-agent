T94 — CI dependency scan and visible digest on CLI. Do ONLY this directive. Do not start T95. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 920ec2b. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not add Electron. Do not weaken T56–T93. English only.

Baseline on main: T01–T93. HEAD 920ec2b. pytest was 586 green after T93.

GOAL: CI runs a dependency or code scan, and `aegis propose` / `aegis status` print payload_sha256 so approve does not require guessing.

Implement:

1) GitHub Actions or existing CI workflow
- keep ruff + pytest
- add one of: pip-audit OR bandit
- do not fail the suite on a scanner that is not installed in the local venv unless you add it as a declared extra
- local pytest must still pass without network

2) core/twin_local.py
- propose success output includes payload_sha256 on its own line
- status queue section includes digest for pending items when present

3) tests/test_t94_digest_visible.py
- AEGIS_DATA_DIR temp
- test_propose_prints_digest
- test_status_shows_pending_digest
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; print(Path('tests/test_t94_digest_visible.py').is_file())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 586
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t94): CI scan and visible approve digest"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
