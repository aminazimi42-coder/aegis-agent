T73 — Mutated payload after approve cannot execute. Do ONLY this directive. Do not start T74. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend af217ce. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, desktop UI, or a license server. Do not weaken T56–T72. English only.

Baseline on main: T01–T72. HEAD af217ce. pytest was 507 green after T72.

GOAL: if the stored action payload changes after approve, execute() refuses. The bound digest from T56 stays the only key.

Implement:

1) core/twin_actions.py execute()
- Recompute the envelope digest from the current stored payload
- If it does not match approved_payload_sha256, raise ValueError and leave status approved
- Do not write a receipt on mismatch

2) tests/test_t73_mutated_payload.py
- AEGIS_DATA_DIR temp
- test_execute_rejects_mutated_payload
- test_status_stays_approved_on_mismatch
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_actions import execute; print(callable(execute))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 507
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t73): reject execute when payload mutated"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
