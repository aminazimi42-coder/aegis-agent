T102 — Local guards: dead-man, calendar overlap, expense dup, inbox rules, transcript flag, delegate close. Do ONLY this directive. Do not start T103. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 4a809d9. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not add Electron. Do not weaken T56–T101. English only.

Baseline on main: T01–T101. HEAD 4a809d9. pytest was 614 green after T101.

GOAL: warn and flag. Do not auto-send mail. Do not auto-resolve.

Implement:

1) scheduler dead-man
- if last tick older than 48h, next aegis status prints a warning line
- missing tick file is also a warning

2) focus-block overlap
- if a propose title/payload has a time slot that overlaps a local .ics event, set conflict or overlap flag
- still propose-only

3) expense duplicate
- two receipts with same amount+vendor+date get duplicate=true
- detection only

4) inbox score
- score from Day-0 profile keywords only, inspectable
- no cloud model

5) transcript gap
- proposed actions from transcript sources with missing text get confidence=low

6) delegate close
- when a decision-record marks a delegate item done, write a local link back to the pack file if present

7) tests/test_t102_local_guards.py
- test_dead_man_warns
- test_expense_duplicate_flag
- test_transcript_low_confidence
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; print(Path('tests/test_t102_local_guards.py').is_file())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 614
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t102): local guards dead-man overlap duplicate confidence"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
