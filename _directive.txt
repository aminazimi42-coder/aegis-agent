T148 — Pack ships entitlement example only. This is Layer 2 instruction 3 of 3. Do ONLY this directive. Do not start T149. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, a live license server, Grok bots, voice, Windows installer, or a seventh agent. Do not bind 0.0.0.0. Do not weaken T56–T147. English only in repo files. Do not copy $HOME/.aegis. Do not write the word CTO into operator HTML or specialist templates.

Baseline: T147 live f563517e. pytest 817 passed 1 skipped. Operator page shows local tier and expiry, or Echo-limited. Layer 2 closes when the giveable pack includes a dummy example file and honest install text. No live license host.

GOAL: The packed folder contains entitlement.example.json only. INSTALL says the operator copies that example to $HOME/.aegis/entitlement.json on that laptop if they want a local tier file. The pack must not contain a real signed entitlement, a private key, or a license URL.

Implement:

1) entitlement.example.json at repo root (or docs/ if a root JSON would confuse the engine — prefer a pack-only copy generated by the pack script)
- Dummy fields: tenant_id, tier, expires_at, signature_sha256
- Values must be obviously fake (example tenant, example signature)
- Engine must not load this example from the repo path as live entitlement. Live path stays AEGIS_DATA_DIR/entitlement.json

2) scripts/pack_local_operator.sh
- Copy entitlement.example.json into dist/aegis-local-operator/
- Do not pack $HOME/.aegis, .git, _directive.txt, or task_instruction.txt
- Keep T143–T145 start-script and home-copy behavior

3) Packed INSTALL.md
- One Now paragraph: optional local file is $HOME/.aegis/entitlement.json; copy the example and replace dummy values on that laptop; missing file stays Echo-limited
- Do not mention Stripe, a license server URL, or App Store

4) README Now
- One sentence only: the local pack ships entitlement.example.json; live entitlement stays under AEGIS_DATA_DIR and is never packed
- Do not claim Layer 3 installer or paid cloud quota

5) tests/test_t148_pack_entitlement_example.py
- test_pack_includes_entitlement_example
- test_pack_omits_live_aegis_dir_and_directive
- test_example_is_not_loaded_as_live_entitlement
- Do not start uvicorn as a subprocess

Verification, exact order. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   bash scripts/pack_local_operator.sh >/tmp/t148-pack.out
   ./.venv/bin/python -c "from pathlib import Path; r=Path('dist/aegis-local-operator'); print((r/'entitlement.example.json').is_file(), (r/'_directive.txt').exists(), (r/'.aegis').exists())"
   Required: True False False
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t148): pack entitlement example only"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

Stop after push. Do nothing else.
