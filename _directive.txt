T95 — Make RELEASES.md match shipped proof. Do ONLY this directive. Do not start T96. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 2197f76. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not add Electron. Do not weaken T56–T94. English only.

Baseline on main: T01–T94. HEAD 2197f76. pytest was 588 green after T94.

GOAL: RELEASES.md describes the twin that exists. Delete or replace stale orchestrator routes and the old 29-test claim.

Required facts, all true today:
- Surface is /api/v1/twin/* plus local `aegis` CLI.
- Six specialists propose only. Execute needs tenant + actor + digest.
- Default provider Echo. Email is local outbox.
- pytest count must be written as the number this run prints, not 29.
- No /api/v1/agents dispatch telemetry as if they were the product.
- No “production SaaS” or “enterprise-grade” claim.

If a heading already says the opposite, replace it. Do not invent features. Do not change Author or LICENSE.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('RELEASES.md').read_text(); print(('29 tests' not in t.lower()) and ('twin' in t.lower()))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 588
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "docs(t95): RELEASES match shipped twin"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
