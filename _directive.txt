T38 — README of shipped work T01–T37. Do ONLY this directive. Do not start T39. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 7547b45. Do not add new product features. Source and commit message: English only.

Baseline on main: HEAD 7547b45. pytest was 330 green after T37.

GOAL: README.md must name what this tree actually ships after T01–T37. No new claims.

Update README.md only plus tests/test_t38_readme_shipped.py. Keep existing CLI and route tables. Add short honest sections if missing:

- Human approve gate: propose is not execute; email-send writes local outbox only
- Safety: complete_safe / secret redaction / policy exist as local guards (name the real modules if present)
- Errors: twin 400 body has detail + code + request_id (core/api_errors.py)
- CI: .github/workflows/ci.yml runs ruff and pytest on Python 3.11 for push/PR to main
- Persistence truth: SQLite under AEGIS_DATA_DIR; do not claim FinOps/approvals survive restart unless code does
- Default LLM remains EchoProvider
- Test suite exists; do not invent a count — write "run pytest -q" not a stale number
- Do not claim desktop UI, paid LLM default, or live SMTP

tests/test_t38_readme_shipped.py
- README contains "EchoProvider"
- README contains "outbox" and "approve"
- README contains "ci.yml" or "GitHub Actions"
- README contains "request_id" or "TWIN_NO_PROFILE" or "api_errors"
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('README.md').read_text(); print('outbox' in t and 'EchoProvider' in t)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 330
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "docs(t38): README names shipped safety errors CI and approve-outbox"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
