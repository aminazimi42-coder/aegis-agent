T152 — Honest Layer 3 Now vs planned. This is Layer 3 instruction 4 of 4. Do ONLY this directive. Do not start T153. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, a live license host, Grok bots, voice, Windows installer, or a seventh agent. Do not bind 0.0.0.0. Do not weaken T56–T151. English only in repo files. Do not copy $HOME/.aegis. Do not write the word CTO into operator HTML or specialist templates. Do not claim notarization, App Store, or that the owner already installed from the .app.

Baseline: T151 live cf9caa1. pytest 836 passed 1 skipped. Layer 3 code for unsigned installer folder, icon, data-preserving uninstall, and optional license GET is in the repo. Owner and amin have not yet run Install.command as the Layer 3 trial. That trial is tomorrow.

GOAL: README and STATUS tell the truth about what Layer 3 shipped in git versus what is still a laptop trial. No new product behavior.

Implement:

1) README Now
- Replace any sentence that implies a notarized desktop shop, a deployed license server, or that Install.command was already proven on a stranger account.
- Keep prior proven Now: local pack, start_operator.sh, $HOME/.aegis, 127.0.0.1:8741, six specialists, approve/reject, signed export, entitlement.example.json, live entitlement only under AEGIS_DATA_DIR.
- Add only these Layer 3 Now facts: scripts/build_mac_installer.sh writes unsigned dist/AegisOperator-mac; uninstall leaves $HOME/.aegis; optional AEGIS_LICENSE_STATUS_URL is a labeled GET default off.
- One Planned sentence: Mac installer trial on owner and amin accounts; notarize only with a Developer ID; license host remains outside this repo.

2) STATUS.md
- One line: Layer 3 git slices T149–T152 landed; installer laptop trial not yet recorded.

3) Packed INSTALL.md text produced by scripts/build_mac_installer.sh
- Keep unsigned / not App Store.
- Do not add a license server URL.

4) tests/test_t152_layer3_truth.py
- test_readme_names_unsigned_installer
- test_readme_does_not_claim_notarized_or_app_store
- test_readme_does_not_claim_license_server_deployed
- test_status_says_trial_not_yet_recorded
- Do not start uvicorn as a subprocess.

Verification, exact order. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; r=Path('README.md').read_text(); s=Path('STATUS.md').read_text() if Path('STATUS.md').is_file() else ''; print('AegisOperator-mac' in r or 'build_mac_installer' in r, 'notarized' not in r.lower(), 'App Store' not in r, 'trial' in s.lower() or 'not yet' in s.lower())"
   Required: True True True True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "docs(t152): honest Layer 3 now vs planned"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

Stop after push. Do nothing else.
