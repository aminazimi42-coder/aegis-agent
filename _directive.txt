T88 — Day-0 interview writes a consented local profile. Do ONLY this directive. Do not start T89. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 194b674. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not add Electron. Do not weaken T56–T87. English only.

Baseline on main: T01–T87. HEAD 194b674. pytest was 563 green after T87.

GOAL: aegis interview records answers under ~/.aegis after explicit consent. Users never touch the git tree.

Implement:

1) core/twin_local.py
- interview TENANT_ID ROLE GOAL CONSENT
- CONSENT must be yes
- Writes profile keys role, goal, consented=true under data_root / tenant
- If CONSENT is not yes, write nothing and exit 2
- After success, status HOME must not say "no consented profile"
- No secrets, no card fields, no network

2) tests/test_t88_interview.py
- AEGIS_DATA_DIR temp
- test_yes_consent_writes_profile
- test_no_consent_writes_nothing
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_local import main; print(callable(main))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 563
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t88): day-zero interview consented local profile"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
