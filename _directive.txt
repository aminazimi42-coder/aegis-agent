T99 CONTINUE — Finish verification and push only. Do not start T100. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add features. English only.

The reject/budget/conflict code is already in the tree. One SELECT patch was applied so reject_reason loads. Do not redesign.

Run exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. ./.venv/bin/python -m pytest -q tests/test_t99_reject_budget_conflict.py
   Required: 0 failed
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 600
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t99): reject reasons specialist budget conflict flag"
7. git push origin main
8. Print new SHA, pytest count, T99 file-test result.

Stop after push. Do nothing else.
