T87 — Install aegis command outside the git tree. Do ONLY this directive. Do not start T88. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend c94c651. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not add Electron. Do not weaken T56–T86. English only.

Baseline on main: T01–T86. HEAD c94c651. pytest was 556 green after T86.

GOAL: scripts/install_local.sh installs a user command at ~/.local/bin/aegis that does not write into the repo.

Implement:

1) scripts/install_local.sh
- POSIX sh, starts with #!/bin/sh
- PREFIX="${AEGIS_PREFIX:-$HOME/.local/aegis}"
- BIN="${AEGIS_BIN:-$HOME/.local/bin}"
- Create PREFIX/venv with python3 -m venv
- pip install -e the repo root into that venv
- Write BIN/aegis that execs PREFIX/venv/bin/python -m core.twin_local "$@"
- mkdir -p PREFIX and BIN
- Do not write into the git working tree except this script

2) tests/test_t87_install_local.py
- test_install_script_exists
- test_script_targets_home_local_not_repo
- Forbidden in script body: Stripe, desktop_engine
- Do not run the installer against the real home
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; print(Path('scripts/install_local.sh').is_file())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 556
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t87): install aegis command outside git tree"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
