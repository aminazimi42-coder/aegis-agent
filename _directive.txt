T83 — Quota store has no card fields. Do ONLY this directive. Do not start T84. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend b0f7049. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not weaken T56–T82. English only.

Baseline on main: T01–T82. HEAD b0f7049. pytest was 542 green after T82.

GOAL: the quota JSON and twin_quota.py source never store card, stripe, payment, or customer identifiers.

Implement:

1) core/twin_quota.py
- get_quota / set_quota keys remain only remaining and period_end
- Reject extra keys on write by dropping them
- Do not add a payment module

2) tests/test_t83_quota_no_card.py
- AEGIS_DATA_DIR temp
- test_quota_keys_only_remaining_and_period_end
- test_twin_quota_source_has_no_card_fields
- Forbidden substrings in core/twin_quota.py source: stripe, card_number, payment_id, customer_id
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_quota import get_quota; print(set(get_quota('t83').keys()) <= {'remaining','period_end'})"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 542
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t83): quota store rejects card fields"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
