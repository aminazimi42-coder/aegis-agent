T92 — Tag and gate untrusted external text. Do ONLY this directive. Do not start T93. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend e6fbf85. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not add Electron. Do not weaken T56–T91. English only.

Baseline on main: T01–T91. HEAD e6fbf85. pytest was 577 green after T91.

GOAL: .eml and other external text enter the twin only with provenance=untrusted. Injection-shaped content cannot become an executable payload.

Implement:

1) core module for untrusted ingest (reuse policy/allow-list if one exists)
- mark source provenance untrusted
- reject payload that contains execute/approve instruction patterns from mail or raw external text
- trusted local profile.json is not untrusted
- no network

2) tests/test_t92_untrusted_ingest.py
- test_eml_marked_untrusted
- test_injection_shaped_mail_not_executable
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; print(Path('tests/test_t92_untrusted_ingest.py').is_file() or True)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 577
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t92): untrusted ingest provenance and inject gate"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
