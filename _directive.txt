T07 — Production integrity capstone. Do ONLY this directive. Do not start T08. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 62af1ba. Do not rewrite README. Do not add payments, OAuth, Telegram, or desktop UI. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T06. pytest was 145 green after T06.

GOAL: one honest platform status surface that a CTO can trust, plus a single end-to-end test that walks interview -> event -> digest -> propose -> approve -> execute without network.

Implement:

1) core/platform_status.py
- platform_status() -> dict with exactly these keys:
  version (from config, 1.0.0-rc1)
  agent_count (6)
  agents (list of six names in registry order)
  llm_provider (get_provider class name or AEGIS_LLM_PROVIDER default "echo")
  twin_routes (bool True if the FastAPI app object can be inspected OR just True if modules twin_interview, twin_events, twin_actions, twin_git_observer import)
  persistence ("sqlite")
- No marketing claims. No "enterprise AGI" strings.

2) API
- GET /api/v1/platform/status  returns platform_status()
Keep every existing twin route working (session, profile, events, digest, observe/git, actions).

3) tests/test_t07_production_integrity.py
- platform_status version 1.0.0-rc1, agent_count 6, agents include Ahmad and Amin, llm_provider contains echo (case-insensitive)
- FastAPI GET /api/v1/platform/status 200
- E2E isolated AEGIS_DATA_DIR:
  start+6 answers+commit consent True
  ingest git commit event + evolve
  weekly_digest event_count >= 1
  propose_actions >= 1
  execute before approve raises PermissionError
  approve then execute status executed
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.platform_status import platform_status; s=platform_status(); print(s['version'], s['agent_count'], s['llm_provider'])"
   Required: 1.0.0-rc1 6 and echo somewhere in the provider string
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 145
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t07): honest platform status and twin e2e integrity test"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
