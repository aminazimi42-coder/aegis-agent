T118 — Local desk brief tick, readonly scope guard, JSONL audit, fa locale render. Do ONLY this directive. Do not start T119. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 0f991f7. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, SMTP, Gmail, Slack, webhooks, push, launchd, system cron, Postgres, or a live paid LLM. Do not bind 0.0.0.0. Do not let any specialist execute. Do not open a write OAuth scope. Do not weaken T56–T117. English only in repo files.

Baseline on main: T01–T117. HEAD 0f991f774c1f252b5e5630a1e5a29bb4e1eb6592. pytest was 687 passed, 1 skipped after T117.

GOAL: Deepen the existing UTC scheduler into a render-only local brief. Guard observe tokens to readonly scopes if a token appears. Write one JSONL audit line per cycle event under AEGIS_DATA_DIR. Add a fa-IR render helper for operator copy only. No new product surface.

Implement:

1) Desk brief
- Reuse the T68 UTC scheduler. Do not install OS cron or launchd.
- Add render-only brief writer: markdown file under AEGIS_DATA_DIR/briefs/, no network.
- If the last tick is older than 48 hours, print a local warning on the next CLI/home load only.

2) Readonly scope guard
- core/oauth_scope_guard.py (or equivalent)
- Allow only scopes that end with .readonly or are already the local git-observe path
- Reject tokens that contain gmail.send, calendar.events, or any write scope before persist
- Do not build a live OAuth client or Keychain integration

3) Audit JSONL
- core/audit_logger.py
- One JSON object per propose/approve/reject/execute/purge/replay
- correlation_id shared across one action_id
- File path: AEGIS_DATA_DIR/audit/YYYY-MM-DD.jsonl
- No telemetry egress

4) fa locale render
- Small helper core/render_locale.py
- locale=en stays default
- locale=fa-IR only flips template strings for brief/home copy
- No Jalali calendar product
- Logic of propose/approve/execute stays locale-agnostic

5) tests/test_t118_desk_audit_locale.py
- test_brief_render_writes_local_file
- test_write_scope_rejected
- test_audit_line_has_correlation_id
- test_fa_locale_does_not_change_digest
- No live network except TestClient

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('tests/test_t118_desk_audit_locale.py').read_text(); print('readonly' in t.lower() and 'correlation' in t.lower() and 'fa' in t.lower())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 687
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t118): desk brief audit locale and readonly guard"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
