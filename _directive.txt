T147 — Operator page entitlement truth. This is Layer 2 instruction 2 of 3. Do ONLY this directive. Do not start T148. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, a live license server, Grok bots, voice, Windows installer, or a seventh agent. Do not bind 0.0.0.0. Do not weaken T56–T146. English only in repo files. Do not write the word CTO into operator HTML or specialist templates.

Baseline: T146 live eeb8899. pytest 814 passed 1 skipped. Entitlement.json is signed and tenant-bound. Missing, expired, mutated, or foreign-tenant files stay Echo-limited. The operator page does not yet show that truth in one short line.

GOAL: The local operator page shows one honest entitlement line: current tier and expiry when valid, or the Echo-limited phrase when load() degrades. No prices. No Stripe. No cloud-quota claim.

Implement:

1) Operator HTML only
- One short visible line on the home/operator page
- Valid: tier plus expires_at when present, or tier plus no-expiry wording when expires_at is null
- Degraded: a typed Echo-limited phrase. Use the reason from entitlement load when it exists (missing, expired, signature, tenant). Do not invent a second policy.
- Do not add a pay button, upgrade modal, or card field

2) Wire the existing load()
- Pass the current twin/session tenant
- Do not change propose/approve/execute
- Do not add a network license check

3) README Now
- One sentence only: the operator page shows local tier and expiry, or Echo-limited when the file is missing, expired, mutated, or bound to another tenant
- Do not claim a paid portal

4) tests/test_t147_operator_entitlement_line.py
- test_valid_professional_line_shows_tier
- test_missing_file_line_is_echo_limited
- test_expired_line_is_echo_limited
- Use TestClient only. Do not start uvicorn as a subprocess

Verification, exact order. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('tests/test_t147_operator_entitlement_line.py').read_text().lower(); print('echo' in t and 'tier' in t and 'expired' in t)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t147): operator page entitlement line"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

Stop after push. Do nothing else.
