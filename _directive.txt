T132 — Professional local Echo tier. Do ONLY this directive. Do not start T133. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 6383cb7. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, SMTP, Gmail, Slack, webhooks, Postgres, speech-to-text, or a live paid LLM. Do not bind 0.0.0.0. Do not let any specialist execute. Do not weaken T56–T131. English only in repo files.

Baseline on main: T01–T131. HEAD 6383cb76f51ea475717362707132df800d8859881. pytest was 741 passed, 1 skipped after T131.

GOAL: A valid local entitlement with tier=professional is visible on home and does not enable cloud billing. Missing file stays echo. No prices in UI.

Implement:

1) core/entitlement.py
- Accept tier values: echo, professional
- Unknown tier degrades to echo
- Professional does not open outbound LLM or Stripe

2) Operator home markdown
- Line: Tier: professional when file valid
- Line: Tier: echo otherwise
- No EUR, no SKU table

3) tests/test_t132_professional.py
- test_professional_file_shows_tier
- test_unknown_tier_is_echo
- test_home_has_no_price
- No live network except TestClient

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('tests/test_t132_professional.py').read_text().lower(); print('professional' in t and 'price' in t)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 741
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t132): professional local tier label"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
