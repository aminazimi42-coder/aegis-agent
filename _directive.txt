T82 — Zero or expired quota forces Echo. Do ONLY this directive. Do not start T83. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 7638298. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not weaken T56–T81. English only.

Baseline on main: T01–T81. HEAD 7638298. pytest was 539 green after T81.

GOAL: if remaining is 0 or period_end is in the past, complete_safe uses Echo and does not crash.

Implement:

1) core/twin_quota.py
- quota_allows_http(tenant_id: str) -> bool
- False when remaining <= 0
- False when period_end is a past UTC instant
- True otherwise (empty period_end counts as not expired)

2) core/llm_safety.py complete_safe()
- If quota_allows_http is False, use Echo even when AEGIS_LLM_PROVIDER=http
- provider_kind must stay echo
- Do not raise

3) tests/test_t82_quota_forces_echo.py
- AEGIS_DATA_DIR temp
- test_zero_remaining_forces_echo
- test_expired_period_forces_echo
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_quota import quota_allows_http; print(callable(quota_allows_http))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 539
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t82): zero or expired quota forces echo"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
