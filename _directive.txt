T72 — Home lists approved-not-executed apart from pending. Do ONLY this directive. Do not start T73. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend bae84b6. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, desktop UI, or a license server. Do not weaken T56–T71. English only.

Baseline on main: T01–T71. HEAD bae84b6. pytest was 504 green after T71.

GOAL: home.md shows a distinct list for actions that are approved but not yet executed, separate from proposed/pending.

Implement:

1) core/twin_home.py
- When rendering pending/home sections, include an Approved — waiting execute group
- Source is twin_actions only (T58 stays)
- Do not treat executed rows as waiting

2) tests/test_t72_home_approved_waiting.py
- AEGIS_DATA_DIR temp
- test_home_lists_approved_not_executed
- test_home_excludes_executed
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_home import render_home; print(callable(render_home))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 504
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t72): home lists approved waiting execute"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
