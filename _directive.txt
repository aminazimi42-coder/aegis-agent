T11 — Versioned behavioral memory from twin events. Do ONLY this directive. Do not start T12. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 016c072. Do not rewrite README. Do not add OAuth, desktop UI, or payments. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T10. pytest was 168 green after T10.

GOAL: turn accumulated events into a versioned habit snapshot the twin can read. This is the machinery for a multi-month twin; it does not invent months of data.

Implement:

1) core/twin_behavior.py
- rebuild(tenant_id) -> dict:
  tenant_id, version (int), event_count, sources {name: count}, repos list unique, top_subjects list up to 5 from commit subjects, updated_at
- Require consented profile or raise ValueError("no consented profile")
- Derive only from twin_events for that tenant (list_events or SQL)
- Persist table twin_behavior (tenant_id unique, version, snapshot JSON, updated_at)
- If snapshot equal to last stored snapshot: keep version, else version+1
- get_behavior(tenant_id) -> latest snapshot or None

2) API
- POST /api/v1/twin/behavior/rebuild body {tenant_id}
- GET  /api/v1/twin/behavior/{tenant_id}  200 or 404
Keep existing twin routes.

3) tests/test_t11_twin_behavior.py
- AEGIS_DATA_DIR temp
- rebuild without profile raises ValueError
- after T03 commit + one git ingest_event: rebuild event_count >= 1, version == 1
- second rebuild with no new events: version stays 1
- GET FastAPI after rebuild 200
- No live network / no LLM required

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_behavior import rebuild, get_behavior; print(callable(rebuild), callable(get_behavior))"
   Required: True True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 168
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t11): versioned behavioral memory from twin events"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
