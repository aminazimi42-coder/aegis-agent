T143 — Layer-1 local operator pack. This is pack slice 1 of 2 (T143 pack, T144 copied-folder start). Do ONLY this directive. Do not start T144. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not copy $HOME/.aegis. Do not add Stripe, Grok bots, voice, Windows installer, or a seventh agent. Do not bind 0.0.0.0. Do not weaken T56–T142. English only in repo files.

Baseline: T142 live 47c096d34643cfffabde63feefe42ba37e236681. pytest 797 passed 1 skipped. Owner will later copy this pack onto the other Mac account and start with Terminal only.

GOAL: One script builds dist/aegis-local-operator/ that contains the files needed to start the local operator without git, Hermes, or VS Code. The pack must not contain builder directives or tenant data.

Implement:

1) scripts/pack_local_operator.sh
- Executable.
- Recreates dist/aegis-local-operator/ from repo root.
- Copy the runtime tree needed by start_operator.sh (app/, core/, agents/ if required, scripts/start_operator.sh, pyproject.toml, uv.lock, and any HTML the server serves).
- Do not copy .git, .venv, _directive.txt, task_instruction.txt, .kms/, $HOME/.aegis, Hermes session files.
- Write dist/aegis-local-operator/INSTALL.md with only Now steps: need Python 3.11, create venv, install from lockfile, run ./scripts/start_operator.sh, open http://127.0.0.1:8741/ yourself. Data dir is $HOME/.aegis. No cloud URL.
- Print the output folder path and stop.

2) README Now
- One sentence: Layer-1 pack is built by scripts/pack_local_operator.sh into dist/aegis-local-operator/.
- Do not claim a notarized installer or Windows support.

3) tests/test_t143_local_pack.py
- test_pack_script_exists
- test_pack_excludes_git_and_directive
- test_install_md_names_start_operator
- No live network except TestClient.

Verification, exact order. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; print(Path('scripts/pack_local_operator.sh').is_file())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t143): layer-1 local operator pack script"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

Stop after push. Do nothing else.
