T97 — Secret-shape redaction and typed confirm for L2/L3. Do ONLY this directive. Do not start T98. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 4711a43. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not add Electron. Do not weaken T56–T96. English only.

Baseline on main: T01–T96. HEAD 4711a43. pytest was 593 green after T96.

GOAL: redact more than two env names, and L2/L3 execute needs a typed phrase.

Implement:

1) core redaction (extend existing redact_secrets)
- detect AWS-like keys, JWT-shaped tokens, bearer/sk- prefixes
- local only, no network DLP

2) L2/L3 execute
- require an extra CLI argument CONFIRM exactly equal to CONFIRM
- missing or wrong phrase: write nothing, exit 2
- L0/L1 unchanged
- digest lock stays

3) tests/test_t97_secrets_and_confirm.py
- test_redacts_jwt_and_bearer
- test_l3_without_confirm_writes_nothing
- test_l3_with_confirm_allowed_path
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; print(Path('tests/test_t97_secrets_and_confirm.py').is_file())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 593
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t97): secret-shape redaction and L2 L3 typed confirm"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
