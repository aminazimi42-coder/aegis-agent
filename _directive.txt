T128 — Local desktop start bundle. Do ONLY this directive. Do not start T129. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend f4a40c4. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, SMTP, Gmail, Slack, webhooks, Postgres, or a live paid LLM. Do not bind 0.0.0.0. Do not let any specialist execute. Do not weaken T56–T127. English only in repo files.

Baseline on main: T01–T127. HEAD f4a40c4a3515492c03d56e61d1dda154e0089f52. pytest was 728 passed, 1 skipped after T127.

GOAL: One documented command starts the local engine and points the operator at it. The packaged app must not pretend it is a store-signed product. A stranger still needs this repo or a copied bundle for this slice; T129 will persist session.

Implement:

1) scripts/start_operator.sh
- Reuse scripts/run_local.sh or call the same uvicorn bind as today
- Print one English line: engine URL and that the .app is a wrapper
- Exit non-zero if venv or app.html is missing
- Do not curl a cloud host

2) desktop/macos/Aegis.app
- Keep T122 offline banner
- If a start helper exists in Contents/MacOS, it may exec start_operator.sh
- Do not require sudo
- Do not write outside AEGIS_DATA_DIR except the existing app bundle

3) STATUS.md one line
- Start path is scripts/start_operator.sh
- Do not claim notarized installer or paid SKU

4) tests/test_t128_start_bundle.py
- test_start_script_exists
- test_script_mentions_run_local_or_uvicorn
- test_status_not_notarized_claim
- No live network except TestClient

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; print(Path('scripts/start_operator.sh').is_file())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 728
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t128): operator start bundle"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
