T136 CONTINUE — finish the interrupted extras slice. Do ONLY this file. Do not start T137. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, speech-to-text, microphone, cloud TTS, or a live paid LLM. Do not bind 0.0.0.0. Do not let any specialist execute. Do not weaken T56–T135. Do not revert committed T135. English only.

Baseline: T01–T135 on main at 9eb89fa. pytest was 755 passed, 1 skipped. A previous session ran out of Nous credits while editing README, STATUS, app.html, and tests/test_t136_extras.py. Those edits are NOT committed. Finish them. Do not start over. Do not re-implement T135 weekly brief.

GOAL: complete original T136 only.
1) Optional AGENT_LLM_BACKEND=ollama is a label on the existing T116 HTTP adapter. Echo stays default. Unreachable HTTP falls back to Echo and labels provider=echo(fallback). No Ollama binary. No live host in tests.
2) Interview stays typed. No speech import in app/ or desktop/.
3) Export buyer one-pager writes a .md under AEGIS_DATA_DIR or $HOME/.aegis/exports/ from saved profile plus honest shipped facts only. Empty profile → typed 400, no file. Never write the export into the git worktree.

If a file is already half-edited, finish it. Do not duplicate routes or buttons. Do not invent a third LLM gateway.

tests/test_t136_extras.py must exist and include:
- test_default_provider_still_echo
- test_ollama_alias_falls_back_to_echo_when_unreachable
- test_no_speech_to_text_import
- test_empty_profile_no_onepager
- test_profile_present_writes_onepager_outside_repo
- test_onepager_not_added_to_git_worktree
No live network except TestClient.

README Now: at most two honest sentences already drafted in the interrupted edit. Keep them if they are true. Do not claim GA.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('tests/test_t136_extras.py').read_text().lower(); print(('echo' in t) and ('speech' in t) and ('empty' in t))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 755
5. git add -A (do not force-add .kms/ or data/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t136): optional ollama alias, typed interview lock, local one-pager"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
