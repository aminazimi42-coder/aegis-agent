T16 — LLM secret isolation and complete ledger. Do ONLY this directive. Do not start T17. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 65d40f9. Do not rewrite README. Do not add payments, OAuth, or desktop UI. Do not call a live paid network. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T15. HEAD 65d40f9. pytest was 193 green after T15.

GOAL: API keys and PAT values must never enter the model prompt or the evidence ledger. Every complete_safe call must leave an auditable ledger row when the ledger is available.

Implement:

1) core/llm_safety.py
- SECRET_ENV_KEYS = ("AEGIS_LLM_API_KEY", "AEGIS_GITHUB_TOKEN")
- redact_secrets(text) -> str: for each set env value with len>=8, replace all occurrences with "***"
- complete_safe must redact the prompt before get_provider().complete
- After complete (ok True or False), best-effort EvidenceLedger().append_entry with action="llm_complete_safe", payload without raw key: tenant_id, model, ok, tool, prompt_tokens, completion_tokens. If ledger import/append fails, swallow and still return the complete dict
- Module source must not contain a hardcoded sk- key

2) tests/test_t16_llm_isolation.py
- os.environ AEGIS_LLM_API_KEY=sk-test-isolation-key-123456
- complete_safe prompt that includes that key: returned text must not contain sk-test-isolation-key-123456 (Echo would otherwise echo it)
- core/llm_safety.py source has no sk-
- AEGIS_DATA_DIR temp; no live network

Keep T15 tests green. Do not weaken the tool cage.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "import os; os.environ['AEGIS_LLM_API_KEY']='sk-test-isolation-key-123456'; from core.llm_safety import complete_safe, redact_secrets; t=complete_safe('use sk-test-isolation-key-123456', tenant_id='t16')['text']; print('sk-test-isolation-key-123456' not in t, '***' in redact_secrets('sk-test-isolation-key-123456'))"
   Required: True True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 193
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t16): redact llm secrets and ledger complete_safe calls"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
