T05 — Local git observer for the twin. Do ONLY this directive. Do not start T06. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend b37da48. Do not rewrite README. Do not add GitHub OAuth, tokens, payments, Telegram, or desktop UI. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T04. Six agents, Echo LLM, Day-0 interview, event ingest + evolve + weekly digest. pytest was 128 green after T04.

GOAL: the twin must learn from a real local git working copy without the network. Scan commits, map each to ingest_event + evolve. No GitHub API.

Implement:

1) core/twin_git_observer.py
- observe_repo(tenant_id, repo_path, *, max_commits=20) -> dict {tenant_id, repo, ingested, skipped, latest_sha}
- Require a consented twin profile or raise ValueError("no consented profile")
- Require repo_path/.git or raise ValueError("not a git repo")
- Use subprocess git log -n max_commits --format=%H%x09%ad%x09%s with GIT_TERMINAL_PROMPT=0. No network remotes fetch
- For each commit: ingest_event(source="git", kind="commit", payload={repo: basename or path name, sha, subject, committed_at}) then evolve(tenant_id, event)
- Dedup by sha already stored in twin_events payload (do not ingest same sha twice)
- Return ingested count of new shas only

2) API
- POST /api/v1/twin/observe/git body {tenant_id, repo_path, max_commits}
Keep T03/T04 routes working.

3) tests/test_t05_twin_git_observer.py
- Isolate AEGIS_DATA_DIR temp
- Build a tiny temp git repo with two commits (git init, config user, commit)
- Without profile: observe_repo raises ValueError
- After T03-style commit: observe_repo ingested >= 1, weekly_digest event_count >= 1, profile repositories mentions the temp repo name
- Second observe_repo on same repo ingested == 0 (dedup)
- FastAPI client 200 for observe/git
- No live network / no GitHub token

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof: create a temp git repo in the proof script, run interview+commit, observe_repo, print ingested and second ingested
   Required: first ingested >= 1 and second ingested == 0
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 128
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t05): local git observer feeds twin events"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
