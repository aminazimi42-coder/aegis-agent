T149 — Mac installer artifact separate from the repo. This is Layer 3 instruction 1 of 4. Do ONLY this directive. Do not start T150. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, a live license server, Grok bots, voice, Windows installer, or a seventh agent. Do not bind 0.0.0.0. Do not weaken T56–T148. English only in repo files. Do not copy $HOME/.aegis. Do not write the word CTO into operator HTML or specialist templates. Do not claim notarization, App Store, or Developer ID.

Baseline: T148 live 449b1d34. pytest 820 passed 1 skipped. Giveable pack is dist/aegis-local-operator with root start_operator.sh and entitlement.example.json. Operator still starts that folder from Terminal. Layer 3 starts with an installer artifact that is not the git repo.

GOAL: One script builds a Mac installer folder next to the existing pack. A stranger can copy that folder, run one installer command, and later start with the same start_operator.sh. The installer does not open Safari. The installer does not pack .git, Hermes files, or live entitlement.

Implement:

1) scripts/build_mac_installer.sh
- Executable.
- First runs scripts/pack_local_operator.sh so dist/aegis-local-operator is fresh.
- Writes dist/AegisOperator-mac/ by replacing that folder each run, never appending.
- Copies the packed tree to dist/AegisOperator-mac/aegis-local-operator/.
- Writes dist/AegisOperator-mac/Install.command that:
  - copies aegis-local-operator into $HOME/aegis-local-operator if that home copy is missing
  - does not overwrite an existing $HOME/aegis-local-operator unless the operator passes --replace
  - does not copy $HOME/.aegis
  - does not open a browser
  - prints three English lines: install target, data dir $HOME/.aegis, start is ./start_operator.sh after venv
- Writes dist/AegisOperator-mac/Aegis Operator.app as a minimal unsigned bundle:
  - Contents/Info.plist with CFBundleName Aegis Operator and CFBundleIdentifier lab.azimi.aegis.operator
  - Contents/MacOS/AegisOperator executable script that sets AEGIS_DATA_DIR=${AEGIS_DATA_DIR:-$HOME/.aegis} and execs the adjacent pack start_operator.sh
  - Does not open Safari
  - Does not call codesign or notarytool
- Do not pack .git, _directive.txt, task_instruction.txt, .kms, or any live $HOME/.aegis

2) dist/AegisOperator-mac/INSTALL.md generated by the same build script
- Now steps only: copy this folder; run Install.command or copy aegis-local-operator into $HOME; python3.11 -m venv .venv; pip install -e .; ./start_operator.sh; open http://127.0.0.1:8741/ yourself.
- One honest line: this app is unsigned; macOS Gatekeeper may warn; this is not App Store and not notarized.
- Data stays in that account $HOME/.aegis.

3) README Now
- One sentence: Layer 3 installer folder is dist/AegisOperator-mac from scripts/build_mac_installer.sh; unsigned; Terminal venv still required tonight.
- Do not claim a paid desktop shop or that owner already installed from the .app.

4) tests/test_t149_mac_installer.py
- test_build_script_exists_and_executable
- test_installer_folder_contains_pack_and_install_command
- test_app_bundle_has_plist_and_macos_stub
- test_installer_omits_git_directive_and_live_aegis
- test_install_md_says_unsigned_and_does_not_open_safari
- test_readme_names_aegisoperator_mac
- Do not start uvicorn as a subprocess.
- Do not run Install.command against the builder home.

Verification, exact order. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   bash scripts/build_mac_installer.sh >/tmp/t149-build.out
   ./.venv/bin/python -c "from pathlib import Path; r=Path('dist/AegisOperator-mac'); print(r.is_dir(), (r/'Install.command').is_file(), (r/'Aegis Operator.app/Contents/MacOS/AegisOperator').is_file(), (r/'aegis-local-operator/start_operator.sh').is_file(), (r/'_directive.txt').exists())"
   Required: True True True True False
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t149): unsigned Mac installer folder"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

Stop after push. Do nothing else.
