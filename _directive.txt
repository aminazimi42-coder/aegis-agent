T64 — Why-replay survives process restart. Do ONLY this directive. Do not start T65. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 5093962. Do not rewrite LICENSE or README. Do not weaken T56–T63. English only.

Baseline on main: T01–T63. HEAD 5093962. pytest was 471 green after T63.

GOAL: the reason an action was approved or rejected is stored in SQLite and readable after a new connection.

Implement:

1) core/twin_actions.py
- Additive ALTER: why_text TEXT
- approve(..., why: str | None = None) stores why_text
- reject(..., why: str | None = None) stores why_text
- replay_why(action_id: str) -> str reads why_text from a fresh get_connection()
- Empty why is allowed and stored as ""

2) tests/test_t64_why_replay.py
- AEGIS_DATA_DIR temp
- test_approve_why_survives_new_connection
- test_reject_why_survives_new_connection
- No live network

Update older approve/reject call sites only if signatures break.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_actions import replay_why; print(callable(replay_why))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 471
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t64): persist why-replay across restart"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
