T167_CONTINUE — Sign the Mac operator with the live Developer ID identity. This is 1 of 4 remaining money-layer slices after the identity landed on hermesdev. Do ONLY this directive. Do not start T168. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, a card form, a live license host, Grok bots, voice, Windows, WebSockets, Playwright, OS cron, or a seventh agent. Do not bind 0.0.0.0. Do not weaken T56–T166. English only in repo files. Do not copy $HOME/.aegis. Do not write the word CTO into operator HTML or specialist templates. Do not store an Apple password, App Store Connect key, .p8, .p12, .cer, .csr, or payment date in the repo. Do not call notarytool. Do not print notarized. Do not write App Store as shipped.

Baseline: T168 script scaffolding already exists from an earlier slice. hermesdev keychain now has 1 valid identity: Developer ID Application: Amin Azimi (3J54UZPZW3). Team ID 3J54UZPZW3. Certificate expires 2031-09-14. CI runners still have no identity. That is allowed.

GOAL: On a machine that has the identity, scripts/codesign_operator.sh signs dist/AegisOperator-mac/Aegis Operator.app when that app exists, prints SIGNED plus the identity name, and never claims notarized. On CI or a machine without the identity it still prints UNSIGNED and exits 0. Public facts only (Team ID, identity name, expiry year) may appear in STATUS.md and RELEASES.md. Raw certificate files stay out of git.

Implement:

1) scripts/codesign_operator.sh
- Keep the UNSIGNED path when security find-identity -v -p codesigning has no "Developer ID Application" line.
- When an identity exists, sign with:
  codesign --force --deep --options runtime --timestamp --sign "$IDENTITY"
  on dist/AegisOperator-mac/Aegis Operator.app if present, else print APP_MISSING and exit 0.
- Prefer AEGIS_CODESIGN_IDENTITY when that env is set and non-empty; otherwise use the first Developer ID Application identity.
- After sign, run codesign --verify --deep --strict on the app. Print SIGNED: <identity> only if verify exits 0.
- Do not invoke notarytool. Do not invoke stapler. Print NOTARY_LOCKED_UNTIL_TICKET.
- Never write the word notarized as a success state.

2) docs/APPLE_DEVELOPER.md
- State Team ID 3J54UZPZW3 and identity name Developer ID Application: Amin Azimi.
- State signing happens only on hermesdev, never on amin.
- State raw .cer/.csr/.p12/.p8 stay in Downloads or Keychain, never in git.
- State notarize is a later owner action after a signed app exists; no ticket exists yet.
- No dollar amount. No pay date.

3) STATUS.md and RELEASES.md
- One Now sentence: hermesdev has a valid Developer ID Application identity; the codesign script signs when that identity and the app bundle exist; the shipped tree is not notarized.
- Planned stays labeled Planned: Apple-accepted notarize, live Stripe, cloud license host.
- Do not move notarize into Now.

4) README Now vs Planned
- Now: Developer ID signing is wired; without the identity the script prints UNSIGNED.
- Do not claim the public download is notarized or on the App Store.
- Leave the Author paragraph untouched.

5) tests/test_t167_continue_live_identity.py
- test_script_exists
- test_unsigned_path_still_present
- test_signed_print_requires_verify
- test_script_does_not_call_notarytool
- test_script_prints_notary_locked_until_ticket
- test_docs_name_team_id_3J54UZPZW3
- test_docs_say_hermesdev_not_amin
- test_readme_author_untouched
- test_readme_does_not_claim_notarized_or_app_store
- test_repo_tracks_no_cer_csr_p8_p12
- Do not run codesign against a missing app as a failing test. Do not run notarytool. Do not start uvicorn. Do not require the identity in CI.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. If the tool budget is low after pytest, skip commentary and do git commit + push immediately.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; s=Path('scripts/codesign_operator.sh').read_text() if Path('scripts/codesign_operator.sh').is_file() else ''; d=Path('docs/APPLE_DEVELOPER.md').read_text() if Path('docs/APPLE_DEVELOPER.md').is_file() else ''; r=Path('README.md').read_text(); print(Path('scripts/codesign_operator.sh').is_file(), 'UNSIGNED' in s, 'SIGNED' in s, 'notarytool' not in s.lower() or 'NOTARY_LOCKED_UNTIL_TICKET' in s, '3J54UZPZW3' in d, 'hermesdev' in d.lower(), 'notarized' not in r.lower())"
   Required: True True True True True True True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add dist/, .kms/, *.cer, *.csr, *.p8, *.p12, or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t167): sign operator with live Developer ID identity"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

Stop only after a successful push. Do nothing else.
