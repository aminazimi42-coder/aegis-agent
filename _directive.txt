T175_CONTINUE — Finish T175 only. Do not start T176. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, voice, Windows, a seventh agent, or a signed-app claim. English only in repo files. Do not copy $HOME/.aegis.

HARD STOP: pytest then commit then push. After push, stop.

Fix only this:

1) tests/test_t175_receipt_depth.py
- Repair the syntax error. tearDown must be valid Python, for example:
  def tearDown(self) -> None:
- Do not delete the nine tests. Do not invent extra product.

2) Then run this exact order. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; r=Path('README.md').read_text(); print('correlation' in r.lower() or 'Intact' in r, 'Author' in r, 'notarized' not in r.lower() or 'not notarized' in r.lower())"
   Required: True True True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add dist/, .kms/, *.cer, *.p8, *.p12, or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t175): correlation id, note hash, digest preview, Intact/Tampered"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

After step 4 you MUST run steps 5–8 in the same turn. Stop only after a successful push. Do nothing else.
