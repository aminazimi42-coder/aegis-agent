T161 CONTINUE — finish only. Do not start T162. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not change product behavior beyond what T161 already implemented. English only.

The five tests in tests/test_t161_conflict_why.py already passed. Finish verification and publish.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. ./.venv/bin/python -m pytest -q
   Required: 0 failed. Expected about 888 passed 1 skipped if the five T161 tests are included.
4. Proof:
   ./.venv/bin/python -c "from pathlib import Path; r=Path('README.md').read_text(); print('conflict' in r.lower() and 'why' in r.lower(), Path('tests/test_t161_conflict_why.py').is_file())"
   Required: True True
5. git add -A (do not force-add .kms/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t161): receipt-backed why line and conflict tag depth"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

If already committed, print the existing SHA and stop. Do not invent a second commit. Stop after push.
