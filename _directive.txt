T186 — Public-demo write and raw-PAT deny only. This is compressed-eval item 2 of 2. Do ONLY this directive. Do not start T187. Do not run notarytool, store-credentials, codesign, or the amin trial. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add Grok bots, voice, Windows, WebSockets, Playwright, OS cron, JWT product auth, or a seventh agent. Do not bind 0.0.0.0. Do not weaken T56–T185. English only in repo files. Do not copy $HOME/.aegis. Do not write the word CTO into operator HTML or specialist templates. Do not store STRIPE_SECRET_KEY, webhook secrets, a GitHub PAT, or a payment date in the repo. Do not write NOTARIZED as a shipped claim.

Baseline: T185 712b0f2f0985214954744f5f0fa7b580a8023bd1. pytest 1053 passed 1 skipped. Live demo host https://aegis-agent-haka.onrender.com instance 0.5c-512mb. Product remains local $HOME/.aegis port 8741. Echo-limited on missing entitlement.

GOAL: The public Render demo must not accept a raw GitHub PAT or treat an anonymous cloud hit as the operator twin. Local start_operator.sh on hermesdev/amin is unchanged. No new identity product.

Implement:

1) Demo mode detector (one helper, reuse if a flag already exists)
- Public demo is true when AEGIS_PUBLIC_DEMO is 1/true or RENDER is set in the environment.
- Local operator default is false (unset on laptop).
- Do not detect demo from the hostname string alone inside execute.

2) Deny on public demo only
- POST /api/v1/twin/observe/github (and any sibling that accepts a token/PAT field) returns 403 with a typed code such as TWIN_DEMO_INGEST_DENIED. Do not store the token. Do not call GitHub.
- Other unauthenticated mutating twin writes that ingest untrusted remote credentials follow the same deny when demo is true.
- GET /health, /docs, /metrics stay up.
- Local mode (flag unset): existing observe/propose/approve/execute behavior unchanged.

3) Operator HTML / README Now
- One sentence: the public demo rejects raw PAT ingest; the operator twin is the local page.
- Do not claim the demo is a multi-tenant product.
- Author paragraph untouched.

4) STATUS.md
- One line: T186 public demo denies raw PAT ingest; local loop unchanged.

5) tests/test_t186_public_demo_deny.py
- demo flag on -> observe/github with a fake PAT is 403 and the string is not written to disk
- demo flag off -> existing local path still callable in-process (no live GitHub)
- health still 200 in demo mode if a test client is used
- README does not contain notarized as a shipped claim
- No xfail. Do not start uvicorn. Do not hit aegis-agent-haka.

Verification, exact order. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; print(Path('tests/test_t186_public_demo_deny.py').is_file(), 'T186' in Path('STATUS.md').read_text(), 'notarized' not in Path('README.md').read_text().lower())"
   Required: True True True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add dist/, .kms/, $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t186): public demo denies raw PAT ingest"
7. git push origin main
8. Print new SHA, pytest count, and the three-token proof.

Stop only after a successful push. Do nothing else.
