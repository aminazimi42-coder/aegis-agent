T126 — Neighbor-safe purge, signed local export, conflict tags. Do ONLY this directive. Do not start T127. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend a85992f. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, SMTP, Gmail, Slack, webhooks, Postgres, or a live paid LLM. Do not bind 0.0.0.0. Do not let any specialist execute. Do not weaken T56–T125. English only in repo files.

Baseline on main: T01–T125. HEAD a85992fd1767f6aad73f0aab19d8a8514dcd546b. pytest was 721 passed, 1 skipped after T125.

GOAL: Purge of tenant A leaves tenant B rows and receipts untouched. Local export writes a signed file under AEGIS_DATA_DIR only. Concurrent propose on the same action_id is tagged conflict, not silently merged.

Implement:

1) Purge property
- Reuse T62 typed confirm
- After purge(A), count(B) and receipt files for B are unchanged

2) Signed local export
- File path under AEGIS_DATA_DIR/export/
- Body plus sha256 signature line
- No email, no cloud upload

3) Conflict tag
- Second write to an existing action_id sets conflict=true on the later row or returns 409
- Do not clobber the first payload

4) tests/test_t126_purge_export_conflict.py
- test_purge_a_leaves_b
- test_export_has_sha256
- test_conflict_not_silent_merge
- No live network except TestClient

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('tests/test_t126_purge_export_conflict.py').read_text().lower(); print('purge' in t and 'export' in t and 'conflict' in t)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 721
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t126): neighbor-safe purge and signed export"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
