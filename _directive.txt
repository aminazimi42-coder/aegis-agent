T13 — Local twin CLI operator. Do ONLY this directive. Do not start T14. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 2d32f1a. Do not rewrite README except one short CLI usage paragraph if the file already has a Quick Start. Do not add desktop GUI, OAuth, or payments. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T12. pytest was 182 green after T12.

GOAL: a CTO can drive interview, approve an action, and render work products from the terminal without opening Swagger.

Implement:

1) tools/twin_cli.py
- argparse program: python -m tools.twin_cli OR python tools/twin_cli.py
- Subcommands:
  status
  interview-start --tenant ID
  interview-answer --session ID --question ID --text TEXT
  interview-commit --session ID --consent {true,false}
  actions-propose --tenant ID
  actions-approve --action-id ID
  actions-execute --action-id ID
  render --tenant ID
- Each command prints JSON to stdout (json.dumps default=str) and exit 0 on success
- On ValueError or PermissionError print {"error": str} to stdout and exit 2
- status prints platform_status()
- No network. No input() prompts.

2) tests/test_t13_twin_cli.py
- AEGIS_DATA_DIR temp
- status exit 0 and stdout contains 1.0.0-rc1
- interview-start + 6 answers using QUESTIONS + commit consent true exit 0
- execute before approve exit 2
- propose then approve then execute then render exit 0; weekly_plan.md exists under AEGIS_DATA_DIR
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python tools/twin_cli.py status
   Required: exit 0 and stdout contains 1.0.0-rc1
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 182
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t13): local twin CLI for interview, approve, render"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
