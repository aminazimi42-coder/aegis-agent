T105 — Desktop window wired to the local twin. Do ONLY this directive. Do not start T106. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 89a4b71. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not add Electron. Do not bind 0.0.0.0. Do not weaken T56–T104. English only.

Baseline on main: T01–T104. HEAD 89a4b71. pytest was 634 green after T104.

GOAL: stub.html becomes an operator page for interview, status, and queue against 127.0.0.1 only.

Implement:

1) desktop/macos/Aegis.app/Contents/Resources/app.html
- sections: interview fields, home/status, pending queue
- fetch only http://127.0.0.1:8741 (or the port the launcher already uses)
- no third-party CDN

2) local routes already in the twin
- reuse /health, /api/v1/twin/* if present
- if a needed read route is missing, add the smallest JSON read on 127.0.0.1
- no new cloud calls

3) launcher opens app.html not stub.html

4) tests/test_t105_desktop_window.py
- test_app_html_mentions_interview_and_queue
- test_app_html_uses_loopback_only
- test_health_ok
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('desktop/macos/Aegis.app/Contents/Resources/app.html').read_text(); print(('127.0.0.1' in t) and ('interview' in t.lower()))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 634
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t105): desktop window wired to local twin"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
