T167 — Apple Developer ID wiring only. This is 1 of 4 money-layer slices. Do ONLY this directive. Do not start T168. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, a card form, a live license host, Grok bots, voice, Windows, WebSockets, Playwright, OS cron, or a seventh agent. Do not bind 0.0.0.0. Do not weaken T56–T166. English only in repo files. Do not copy $HOME/.aegis. Do not write the word CTO into operator HTML or specialist templates. Do not store an Apple password, App Store Connect key, or payment date in the repo.

Baseline: T164 live 81c1e7e8f9f6b588da7a9b3c2d8f04212151bd32. pytest 905 passed 1 skipped. T166 amin stranger trial passed 2026-09-13. Owner said start Developer ID. This machine may still have no Developer ID identity. That is allowed.

GOAL: The repo can detect a Developer ID Application identity and sign only when that identity exists. Without it the script still prints UNSIGNED and exits 0. Nothing claims notarized, App Store, or paid. Enrollment itself stays an owner browser action outside Hermes.

Implement:

1) scripts/codesign_operator.sh (edit in place if it exists)
- Default app path: dist/AegisOperator-mac/Aegis Operator.app if present, else print APP_MISSING and exit 0.
- Run `security find-identity -v -p codesigning` when the security tool exists.
- If no line contains "Developer ID Application": print UNSIGNED: no Developer ID identity; do not call codesign --sign with a guessed name; do not call notarytool; exit 0.
- If an identity exists: codesign --force --deep --options runtime --sign "$IDENTITY" --timestamp on the app. Use the first Developer ID Application identity found, or AEGIS_CODESIGN_IDENTITY if that env is set and non-empty.
- Do not submit to notarytool in T167. Print NOTARY_LOCKED_UNTIL_T168.
- Never write "notarized" as success. Never write App Store.

2) docs/APPLE_DEVELOPER.md (new, short, English)
- Owner enrolls at https://developer.apple.com/programs/ on the hermesdev account, not amin.
- Annual fee exists; do not write a dollar amount or a pay date.
- After enrollment the owner creates a Developer ID Application certificate in the Apple account and installs it in the hermesdev keychain.
- Then rerun scripts/codesign_operator.sh from the repo on hermesdev.
- Signing does not happen on the amin operator account.
- No Stripe. No license host URL required for this slice.

3) README Now vs Planned
- Now: codesign script prints UNSIGNED when no Developer ID identity is in the keychain.
- Planned, labeled Planned: Apple-accepted notarize, live Stripe, cloud license host.
- Do not move notarize or Stripe into Now.

4) STATUS.md
- One line: T167 Apple Developer ID wiring started after T166 amin trial; notarize and Stripe still locked.

5) tests/test_t167_codesign_identity.py
- test_script_exists
- test_script_mentions_unsigned
- test_script_does_not_call_notarytool_in_t167 (source must contain NOTARY_LOCKED_UNTIL_T168 and must not invoke notarytool as an unguarded command)
- test_docs_say_enroll_on_hermesdev_not_amin
- test_readme_does_not_claim_notarized_or_app_store
- test_repo_has_no_apple_password_files (fail if a file named *AuthKey_*.p8 or *apple-id*password* is tracked)
- Do not run notarytool against Apple. Do not start uvicorn. Do not require a real certificate in CI.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. If the tool budget is low after pytest, skip commentary and do git commit + push immediately.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; s=Path('scripts/codesign_operator.sh').read_text() if Path('scripts/codesign_operator.sh').is_file() else ''; d=Path('docs/APPLE_DEVELOPER.md').read_text() if Path('docs/APPLE_DEVELOPER.md').is_file() else ''; r=Path('README.md').read_text(); print(Path('scripts/codesign_operator.sh').is_file(), Path('docs/APPLE_DEVELOPER.md').is_file(), 'UNSIGNED' in s, 'NOTARY_LOCKED_UNTIL_T168' in s, 'hermesdev' in d.lower(), 'notarized' not in r.lower())"
   Required: True True True True True True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add dist/, .kms/, *.p8, or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t167): wire Developer ID codesign without notarize claim"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

Stop only after a successful push. Do nothing else.
