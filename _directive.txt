T93 — Hash-chained local audit for propose, approve, execute. Do ONLY this directive. Do not start T94. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 1c55d62. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not add Electron. Do not weaken T56–T92. English only.

Baseline on main: T01–T92. HEAD 1c55d62. pytest was 583 green after T92.

GOAL: each propose, approve, and execute appends one audit line under the tenant data root. Lines chain prev_hash → hash. No network.

Implement:

1) core audit append-only file under data_root / tenant_id / audit.jsonl
- fields: ts, kind (propose|approve|execute), action_id, prev_hash, hash
- hash is SHA-256 of the canonical line including prev_hash
- first line prev_hash is 64 zeros
- never rewrite earlier lines

2) tests/test_t93_audit_chain.py
- AEGIS_DATA_DIR temp
- test_first_line_zero_prev
- test_second_line_binds_prev
- test_rewrite_forbidden_or_detectable
- No live network

Hook the existing twin_local propose/approve/execute paths so a successful call appends a line. Do not change digest-lock semantics.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; print(Path('tests/test_t93_audit_chain.py').is_file())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 583
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t93): hash-chained local audit log"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
