T79 — Local view reports Echo versus HTTP-fallback. Do ONLY this directive. Do not start T80. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 9743b3a. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not weaken T56–T78. English only.

Baseline on main: T01–T78. HEAD 9743b3a. pytest was 528 green after T78.

GOAL: the operator can ask the local view which LLM path is active without opening a socket.

Implement:

1) core/twin_local_view.py
- provider_status() -> dict with keys kind and offline
- kind is "echo" when EchoProvider or offline_mode()
- kind is "http-fallback" only when get_provider() is HTTP and offline is false
- Do not call complete() or open a network connection

2) tests/test_t79_provider_status.py
- AEGIS_DATA_DIR temp
- test_status_echo_when_offline
- test_status_has_kind_key
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_local_view import provider_status; print('kind' in provider_status())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 528
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t79): local provider status echo or http-fallback"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
