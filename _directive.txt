T91 — Tell the truth about shipped capability. Do ONLY this directive. Do not start T92. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 57d3085. Do not rewrite LICENSE. Do not rewrite the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not add Electron. Do not weaken T56–T90. English only.

Baseline on main: T01–T90. HEAD 57d3085. pytest was 577 green after T90.

GOAL: README.md and STATUS.md match proof. No new product claims.

Required statements, all true today:
- Six named specialists propose into one queue. They do not execute.
- Execute needs a human approve bound to tenant + actor + digest.
- Default provider is Echo. HTTP is optional and labeled.
- Email send is local outbox only. No SMTP in core.
- Local data lives under the user data root, not the git tree.
- Cloud Render is optional. Not a production SaaS claim.

Forbidden:
- multi-agent negotiation / debate / LangGraph claims
- coverage percent you did not measure
- “enterprise-grade” or “production SaaS”
- changing Author or LICENSE

If a heading already says the opposite, replace that heading. Do not invent features.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('README.md').read_text()+Path('STATUS.md').read_text(); print('Echo' in t and 'outbox' in t.lower())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 577
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "docs(t91): truth surfaces match shipped twin"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
