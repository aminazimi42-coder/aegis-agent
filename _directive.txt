T137 — Home depth: approved receipt stays visible, live engine-off banner, signed brief export on the operator page. This is 2 of 3 remaining slices. After this file, 1 slice remains. Do ONLY this directive. Do not start T138. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend b6cf9b4. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, SMTP, speech, or a live paid LLM. Do not bind 0.0.0.0. Do not let any specialist execute. Do not weaken T56–T136. English only in repo files.

Baseline on main: T01–T136. HEAD b6cf9b4. pytest was 762 passed, 1 skipped after T136.

GOAL: Depth on the existing operator home. No new pillar.
1) Approve must not make the action disappear from the operator. Pending column stays proposed-only. A second visible strip or column shows approved receipts for this tenant.
2) When the local engine is unreachable or /health fails, the operator page shows a single honest Engine offline banner and points the operator back to the local start path. No fake healthy state.
3) One button exports the signed local weekly-brief pack already defined by T126. File lands under AEGIS_DATA_DIR or $HOME/.aegis/exports/. Never into the git worktree.

Implement:

A) Approved receipt surface
- Pending queue: proposed only (keep T114).
- Add an Approved strip or Archive column that lists approved twin actions for the current tenant after refresh.
- Each row shows agent, title or task excerpt, digest prefix, and approved timestamp.
- Rejected actions do not appear in Approved.
- Do not auto-execute an approved row.

B) Engine offline banner
- Reuse T122 copy if present. One banner, English.
- Probe existing health route. On failure or fetch error, show the banner.
- On success, hide the banner.
- Do not invent a second health URL. Do not bind 0.0.0.0.

C) Signed brief export button
- Label: Export signed brief
- Calls the existing signed export path if one exists. If missing, add the smallest local writer that hashes the brief bytes and writes sidecar digest under the data dir.
- Empty profile or empty brief → typed 400, no file.
- Do not commit export files.

D) tests/test_t137_home_depth.py
- test_pending_hides_approved
- test_approved_strip_shows_approved_action
- test_rejected_not_in_approved_strip
- test_health_ok_hides_offline_banner
- test_health_down_shows_offline_banner
- test_signed_export_writes_outside_repo
- No live network except TestClient.

E) README
- At most two honest Now sentences: approved receipts stay visible on home; offline banner is local health, not cloud SLA.
- Do not rewrite Author. Do not claim GA.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('tests/test_t137_home_depth.py').read_text().lower(); print(('approved' in t) and ('offline' in t or 'banner' in t) and ('export' in t))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 762
5. git add -A (do not force-add .kms/ or data/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t137): approved receipts, offline banner, signed brief export"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
