T135 — First-run weekly brief from saved profile. Do ONLY this directive. Do not start T136. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 8f5f7c4. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, SMTP, speech-to-text, or a live paid LLM. Do not bind 0.0.0.0. Do not let any specialist execute. Do not weaken T56–T134. English only in repo files.

Baseline on main: T01–T134. HEAD 8f5f7c4. pytest was 751 passed, 1 skipped after T134.

GOAL: After a durable profile exists, operator home has one button that proposes a weekly desk brief from name/role/goals/timezone. Same Propose path. Empty profile does not invent a brief.

Implement:

1) desktop/app.html or twin home
- Button: Propose weekly brief
- POST existing propose with a deterministic task string built from saved profile fields
- No microphone
- No new agent

2) If profile empty, button disabled or returns 400 typed error
- Do not fabricate goals

3) tests/test_t135_weekly_brief.py
- test_profile_present_proposes_brief
- test_empty_profile_no_brief
- test_no_voice_hook
- No live network except TestClient

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('tests/test_t135_weekly_brief.py').read_text().lower(); print('brief' in t and 'empty' in t)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 751
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t135): weekly brief from saved profile"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
