T48 — Resume includes profile name. Do ONLY this directive. Do not start T49. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 6a1ec95. Do not rewrite README. Source, comments, commit message: English only.

Baseline on main: T01–T47. HEAD 6a1ec95. pytest was 399 green after T47.

GOAL: resume.md shows Name when the consented profile has a name field. Role stays. Missing name is skipped, not invented.

Implement:

1) core/twin_resume_pack.py
- Read name = profile.get("name") or ""
- If name is non-empty, markdown contains "**Name:**" and that value
- Keep **Role:** when role exists
- Do not invent a name

2) tests/test_t48_resume_name.py
- AEGIS_DATA_DIR temp
- after T03 commit, if profile has no name, render still writes the file
- after T03 commit, if tests can set profile name via existing interview helpers, resume contains that name; if no helper exists, at least render_resume accepts profile name when present in stored profile JSON
- FastAPI resume route still 200
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_resume_pack import render_resume; print(callable(render_resume))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 399
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t48): resume pack prints profile name when present"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
