T24 — Writing style lock from local samples. Do ONLY this directive. Do not start T25. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 097c81e. Do not rewrite README. Do not send email or add payments. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T23. HEAD 097c81e. pytest was 237 green after T23.

GOAL: learn a deterministic style profile from a folder of the principal's .txt/.md samples. No live LLM.

Implement:

1) core/twin_style_lock.py
- lock_style(tenant_id, samples_dir) -> dict {tenant_id, path, sample_count, top_words}
- Require consented profile or raise ValueError("no consented profile")
- Require Path(samples_dir).is_dir() or raise ValueError("samples dir not found")
- Read *.txt and *.md (utf-8, ignore errors)
- Tokenize [a-z0-9]+ lowercase; drop words length < 4
- top_words = 12 most frequent
- Write work_products/{tenant_id}/style_lock.md with Voice notes and the top words
- Overwrite on each call

2) API POST /api/v1/twin/style/lock body {tenant_id, samples_dir}
3) CLI style-lock --tenant ID --dir PATH

4) tests/test_t24_twin_style_lock.py
- AEGIS_DATA_DIR temp
- without profile ValueError
- after T03 commit write sample containing "clarity clarity clarity shipping": file contains clarity
- FastAPI 200
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_style_lock import lock_style; print(callable(lock_style))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 237
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t24): lock writing style from local text samples"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
