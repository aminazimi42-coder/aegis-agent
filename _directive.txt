T117 — Structured reject reasons and deterministic queue priority. Do ONLY this directive. Do not start T118. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 8db9ea5. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, SMTP, Gmail, Slack, webhooks, Postgres, or a live paid LLM. Do not bind 0.0.0.0. Do not let any specialist execute. Do not auto-filter or auto-execute low-confidence rows. Do not weaken T56–T116. English only in repo files.

Baseline on main: T01–T116. HEAD 8db9ea5d3d5fcaaa0a84370b1962ef1c79ac49fe. pytest was 682 passed, 1 skipped after T116.

GOAL: Reject stores a typed reason. Queue order is a transparent linear score. Confidence is a label only. Humans still approve.

Implement:

1) Reject path
- Reuse the existing reject that already needs tenant+actor+digest
- Accept an optional reason enum: WRONG_TIMING, WRONG_RECIPIENT, LOW_CONFIDENCE, POLICY_VIOLATION, DUPLICATE, OTHER
- Default OTHER when omitted so old clients still work
- Persist the reason on the twin_actions row or the existing feedback store from T65
- Mutated digest still rejected

2) Local deterministic stats
- Small module core/feedback_stats.py or extend the T65 store
- Moving approval rate per (specialist, effect_type) only
- Attach confidence_score on the next propose of that pair
- Low score sets a warning flag on the card. Never drops the row. Never executes.

3) Priority
- score = w_risk * risk_level + w_age * age_hours + w_specialist * specialist_weight
- Default weights in a local yaml or constants file under the repo
- Home/list of proposed twin_actions uses this order
- Do not create a second queue

4) tests/test_t117_feedback_priority.py
- test_reject_reason_persists
- test_reject_still_needs_digest
- test_low_confidence_still_queued
- test_priority_orders_higher_score_first
- No live network except TestClient

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('tests/test_t117_feedback_priority.py').read_text(); print('WRONG_TIMING' in t and 'confidence' in t.lower() and 'digest' in t.lower())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 682
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t117): reject reasons and deterministic priority"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
