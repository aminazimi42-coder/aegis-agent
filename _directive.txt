T123 CONTINUE — Finish verification only. Do not start T124. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend HEAD. Do not rewrite LICENSE or the README Author paragraph. Do not add features. English only.

The previous session added RISK_TTL_HOURS, digest_label RISK_L2/RISK_L3, risk_warn that never auto-executes, card digest_label, and tests/test_t123_digest_risk.py. L2 test was switched to insert_specialist_proposal. Work is uncommitted. Full pytest after that fix was not finished.

Do only this sequence. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('tests/test_t123_digest_risk.py').read_text().lower(); print('409' in t and 'l2' in t and 'ttl' in t)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 709
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t123): digest property and risk ttl phrase"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
