T27 — Focus block hold. Do ONLY this directive. Do not start T28. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 3a38c57. Do not rewrite README. Do not talk to Google Calendar. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T26. HEAD 3a38c57. pytest was 261 green after T26.

GOAL: create a local focus-block hold the principal can drop into a calendar. No cloud calendar.

Implement:

1) core/twin_focus_block.py
- create_block(tenant_id, start, duration_min=90, title="Focus") -> dict {tenant_id, path_md, path_ics}
- Require consented profile or raise ValueError("no consented profile")
- duration_min must be int >= 15 else ValueError("invalid duration")
- Write work_products/{tenant_id}/focus_block.md with title, start, duration
- Write work_products/{tenant_id}/focus_block.ics with one VEVENT SUMMARY and DTSTART using the given start string as-is
- Overwrite on each call

2) API POST /api/v1/twin/focus/block body {tenant_id, start, duration_min, title}
   duration_min default 90, title default Focus
3) CLI focus-block --tenant ID --start S [--duration 90] [--title Focus]

4) tests/test_t27_twin_focus_block.py
- AEGIS_DATA_DIR temp
- without profile ValueError
- after T03 commit create_block start=20260903T090000Z: both files exist, ics contains VEVENT and 20260903T090000Z
- FastAPI 200
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_focus_block import create_block; print(callable(create_block))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 261
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t27): local focus-block hold as markdown and ics"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
