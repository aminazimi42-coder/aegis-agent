T21 — Follow-up list from email and meetings. Do ONLY this directive. Do not start T22. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend b180972. Do not rewrite README. Do not send email or add payments. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T20. HEAD b180972. pytest was 216 green after T20.

GOAL: one local list of things a busy principal still owes a response to. Deterministic. No live LLM.

Implement:

1) core/twin_followups.py
- render_followups(tenant_id) -> dict {tenant_id, path, count}
- Require consented profile or raise ValueError("no consented profile")
- Items from twin_events:
  source=email kind=message -> follow-up on subject
  source=calendar kind=meeting -> follow-up after summary
- Dedup by (source, subject/summary)
- Write work_products/{tenant_id}/follow_ups.md with numbered items
- Overwrite on each call

2) API POST /api/v1/twin/followups/render body {tenant_id}
3) CLI followups --tenant ID

4) tests/test_t21_twin_followups.py
- AEGIS_DATA_DIR temp
- without profile ValueError
- after T03 commit + ingest email Budget review: file contains Budget review
- FastAPI 200
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_followups import render_followups; print(callable(render_followups))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 216
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t21): follow-up list from email and meeting events"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
