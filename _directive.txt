T44 — Memory control list and forget. Do ONLY this directive. Do not start T45. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 509f284. Do not rewrite README. Do not call a cloud model. Source, comments, commit message: English only.

Baseline on main: T01–T43. HEAD 509f284. pytest was 377 green after T43.

GOAL: the principal can see what the twin stored and drop one field. No encryption product in this slice.

Implement:

1) core/twin_memory_control.py
- show(tenant_id) -> {tenant_id, path, fields}
- Require consented profile or raise ValueError("no consented profile")
- Write work_products/{tenant_id}/memory.md listing profile keys that exist (name, role, and any other keys present). Skip secrets.
- forget(tenant_id, field) -> same shape
- Persist forgotten field names in SQLite table forgotten(tenant_id, field, at) via twin_persist
- show must omit forgotten fields
- Overwrite memory.md on each show/forget

2) API POST /api/v1/twin/memory/show body {tenant_id}
   POST /api/v1/twin/memory/forget body {tenant_id, field}
3) CLI memory-show --tenant
   CLI memory-forget --tenant --field

4) tests/test_t44_twin_memory_control.py
- AEGIS_DATA_DIR temp
- without profile ValueError
- after T03 commit show markdown contains tenant id or a profile key
- forget("role") then show markdown does not contain a Role heading
- FastAPI 200 on show
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_memory_control import show, forget; print(callable(show))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 377
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t44): memory control list and forget field"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
