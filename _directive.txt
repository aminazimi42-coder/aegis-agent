T156 — Quota ledger outside execute. This is pack instruction 4 of 6. Do ONLY this directive. Do not start T157. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, a card form, a live license host, Apple notarization, Grok bots, voice, Windows installer, or a seventh agent. Do not bind 0.0.0.0. Do not weaken T56–T155. English only in repo files. Do not copy $HOME/.aegis. Do not write the word CTO into operator HTML or specialist templates. Do not claim a paid shop or a deployed license server.

Baseline: T155 live 4764361fef04138857e0651aa687f8709489d6e. pytest 857 passed 1 skipped. Entitlement is a local signed file. Remote status cannot unlock without that file. There is no tenant quota ledger that forces Echo when a local allowance is exhausted.

GOAL: One local quota ledger lives under AEGIS_DATA_DIR. Counting happens outside execute. When the remaining allowance is zero, complete_safe stays on Echo and the operator status says quota_exhausted. Propose and execute do not talk to a billing host.

Implement:

1) core/quota_ledger.py (or extend an existing local budget module — do not fork a second counter)
- File: $AEGIS_DATA_DIR/quota.json scoped by tenant_id.
- Fields: tenant_id, period (calendar month UTC), allowance (int), used (int), updated_at UTC.
- remaining = max(allowance - used, 0).
- increment(tenant_id) adds one used unit only when remaining > 0 and returns the new remaining.
- exhausted(tenant_id) is True when remaining == 0.
- Default allowance for a missing file is 0 (Echo-limited, not an implicit paid pool).
- Never open a socket. Never import Stripe.

2) Wire only at complete_safe / operator status
- If exhausted: complete_safe uses EchoProvider even when AGENT_LLM_BASE_URL is set; label the result echo and quota_exhausted.
- If remaining > 0: existing provider path is unchanged; increment happens after a successful non-Echo completion only.
- Execute and approve do not increment the ledger.
- Do not block local file outbox execute because quota is exhausted.

3) Operator status JSON
- One English field: quota_remaining (int) and quota_state = ok | exhausted | unset.
- Do not print secrets.

4) README Now
- One sentence: a local quota.json under AEGIS_DATA_DIR tracks a monthly allowance; remaining zero keeps complete_safe on Echo; execute still writes local receipts; no payments in core.

5) tests/test_t156_quota_ledger.py
- test_missing_file_remaining_is_zero
- test_increment_stops_at_zero
- test_exhausted_forces_echo_even_if_http_configured
- test_execute_does_not_increment_quota
- test_quota_module_does_not_import_stripe
- Use tmp_path. Do not write the developer's real $HOME/.aegis. Do not start uvicorn.

Verification, exact order. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; p=Path('core/quota_ledger.py'); t=p.read_text() if p.is_file() else ''; r=Path('README.md').read_text(); print(p.is_file() or 'quota.json' in r, 'stripe' not in (t+r).lower(), 'quota_exhausted' in r or 'quota.json' in r)"
   Required: True True True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t156): local quota ledger outside execute"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

Stop after push. Do nothing else.
