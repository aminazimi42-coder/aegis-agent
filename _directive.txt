T121 — Distinct specialist proposals; Ahmad/Amin gate. Do ONLY this directive. Do not start T122. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 027e27a. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, SMTP, Gmail, Slack, webhooks, Postgres, or a live paid LLM. Do not bind 0.0.0.0. Do not let any specialist execute. Do not weaken T56–T120. English only in repo files.

Baseline on main: T01–T120. HEAD 027e27aa8dcc7e8453204f5ff93a4b15b3dc9c67. pytest was 699 passed, 1 skipped after T120.

GOAL: One propose batch still creates six rows. Each row must be a different effect_type or title prefix so the operator can tell the specialists apart. Ahmad and Amin are gate/review cards, not a second Alina brief. Specialists still only propose.

Implement:

1) Keep the existing six names: Bita, Kian, Alina, Aylin, Ahmad, Amin
2) Alina = strategy brief. Kian = execution draft. Bita = insight. Aylin = quality review. Ahmad = security/oversight gate. Amin = finance/cost gate.
3) Titles or kind/effect_type must differ inside one batch_id. Identical copy-paste bodies across all six fail the test.
4) Do not add an orchestrator that executes. Do not drop specialists from the batch.

5) tests/test_t121_specialist_distinct.py
- test_six_rows_one_batch
- test_titles_or_kinds_are_unique_in_batch
- test_ahmad_is_gate_not_alina_clone
- test_amin_is_finance_gate
- No live network except TestClient

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('tests/test_t121_specialist_distinct.py').read_text().lower(); print('ahmad' in t and 'amin' in t and 'unique' in t)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 699
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t121): distinct six-specialist proposals"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
