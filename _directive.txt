T119 — Durable operator profile and session. Do ONLY this directive. Do not start T120. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend b378345. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, SMTP, Gmail, Slack, webhooks, Postgres, or a live paid LLM. Do not bind 0.0.0.0. Do not let any specialist execute. Do not auto Start Session. Do not weaken T56–T118. English only in repo files.

Baseline on main: T01–T118. HEAD b378345. pytest was 692 passed, 1 skipped after T118.

GOAL: Name, role, goals, timezone for tenant local survive quit and engine restart. Opening the operator page prefills those four fields when a committed profile exists. Propose works without forcing a new interview. Do not invent a cloud login.

Implement:

1) Persistence
- Reuse the existing interview/profile store and AEGIS_DATA_DIR
- Commit profile remains the source of truth
- A new process against the same AEGIS_DATA_DIR must read the same four fields

2) desktop/macos/Aegis.app/Contents/Resources/app.html
- On load, GET /api/v1/twin/profile/local and fill the four fields when present
- Do not clear the fields on ordinary refresh
- Do not auto-click Start Session
- If the profile is missing, leave fields empty

3) Session helper if needed
- Small local marker under AEGIS_DATA_DIR only, for example last_tenant=local
- No browser cloud cookie as the source of truth

4) tests/test_t119_durable_profile.py
- test_profile_survives_new_store
- test_html_prefills_from_profile_endpoint
- test_start_session_not_auto
- No live network except TestClient

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('desktop/macos/Aegis.app/Contents/Resources/app.html').read_text().lower(); print('profile' in t and 'timezone' in t and 'start session' in t)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 692
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t119): durable local operator profile"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
