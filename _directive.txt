T120 CONTINUE — Finish verification only. Do not start T121. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend HEAD. Do not rewrite LICENSE or the README Author paragraph. Do not add features. English only.

The previous session already added batch_id, two-column app.html, twin_local_view split, and tests/test_t120_two_column_home.py. Work is unstaged. One T113 regex was updated. No commit. No push.

Do only this sequence. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('desktop/macos/Aegis.app/Contents/Resources/app.html').read_text().lower(); print('archive' in t and 'latest' in t)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 695
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t120): two-column operator home"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
