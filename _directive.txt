T114 VERIFY — run verification only. Do not rewrite the T114 feature. Do not start T115. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. English only.

The previous session applied reject digest, pending filter, Reject button, and profile prefill. It stopped before ruff/pytest/commit because the tool-call cap was hit. Working tree is dirty.

Do only this sequence. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('desktop/macos/Aegis.app/Contents/Resources/app.html').read_text().lower(); print('reject' in t and 'profile' in t)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 667
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t114): reject pending and prefill profile"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
