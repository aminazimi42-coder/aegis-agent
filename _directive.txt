T28 — Travel pack from calendar + docs folder. Do ONLY this directive. Do not start T29. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 3cef063. Do not rewrite README. Do not book flights or pay. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T27. HEAD 3cef063. pytest was 266 green after T27.

GOAL: one page for an upcoming trip: calendar meetings whose summary contains flight/hotel/travel plus files from a local docs folder. No live LLM.

Implement:

1) core/twin_travel_pack.py
- render_pack(tenant_id, docs_dir="") -> dict {tenant_id, path}
- Require consented profile or raise ValueError("no consented profile")
- Meetings: twin_events source=calendar kind=meeting whose summary lower contains flight or hotel or travel
- If docs_dir is a directory, list *.pdf *.txt *.md names (not contents)
- Write work_products/{tenant_id}/travel_pack.md with Itinerary and Documents and line "Do not book or pay without written approval."
- Overwrite on each call

2) API POST /api/v1/twin/travel/render body {tenant_id, docs_dir}
3) CLI travel --tenant ID [--dir PATH]

4) tests/test_t28_twin_travel_pack.py
- AEGIS_DATA_DIR temp
- without profile ValueError
- after T03 commit + ingest calendar "Flight to Berlin": file contains Flight to Berlin and written approval
- FastAPI 200
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_travel_pack import render_pack; print(callable(render_pack))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 266
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t28): travel pack from calendar events and local docs"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
