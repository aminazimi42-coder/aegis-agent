T131 — Local entitlement file only. Do ONLY this directive. Do not start T132. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend e68b09f. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, SMTP, Gmail, Slack, webhooks, Postgres, speech-to-text, or a live paid LLM. Do not bind 0.0.0.0. Do not let any specialist execute. Do not weaken T56–T130. English only in repo files.

Baseline on main: T01–T130. HEAD e68b09f. pytest was 738 passed, 1 skipped after T130.

GOAL: Engine reads a local entitlement file under AEGIS_DATA_DIR. Missing or expired file does not crash. It labels the operator Echo-limited. No payment SDK.

Implement:

1) core/entitlement.py
- Path: AEGIS_DATA_DIR/entitlement.json
- Fields: tier, expires_at (ISO UTC or null), signature_sha256 of canonical body without signature
- load() returns tier="echo" when file missing or expired
- Never call a network host

2) Operator home or STATUS line
- Show tier echo|professional
- Do not show prices
- Do not mention Stripe

3) tests/test_t131_entitlement.py
- test_missing_file_is_echo
- test_expired_is_echo
- test_valid_professional_reads_tier
- No live network except TestClient

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('tests/test_t131_entitlement.py').read_text().lower(); print('echo' in t and 'expired' in t)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 738
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t131): local entitlement file"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
