T141 — Strip CTO from operator product copy. This is slice 2 of 3 (T140 done, T141 copy, T142 export depth). Do ONLY this directive. Do not start T142. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not edit $HOME/.aegis profile data. Do not add Stripe, Grok bots, voice, or a live paid LLM. Do not weaken T56–T140. English only in repo files.

Baseline: T140 live 7de1c948c59576e74293cf01d54220704a429c8. pytest 788 passed 1 skipped. Owner laptop still shows Goals "Local operator twin for daily CTO work" because that string is stored profile plus shipped templates. Product UI must not keep writing CTO.

GOAL: Operator-visible copy and specialist proposal templates no longer contain the word CTO. Elite-user positioning stays out of the running product surface. Saved tenant profile on disk is not rewritten by this slice.

Implement:

1) Product strings
- In operator HTML that GET / serves, and in specialist propose templates / default task phrases / weekly-brief copy inside the repo, replace CTO / C.T.O. / cto as a role label with a neutral phrase: senior operator or local operator.
- Do not invent new marketing. Keep sentences short and true.
- Do not change agent names Alina Kian Bita Aylin Ahmad Amin.
- Do not touch LICENSE or README Author.

2) README Now
- One sentence: operator product copy no longer uses the word CTO.
- Do not claim the owner's already-saved Goals field was mutated.

3) tests/test_t141_no_cto_copy.py
- test_operator_html_has_no_cto: GET / HTML lowercased must not contain "cto"
- test_specialist_templates_have_no_cto: repo template/prompt files used to propose weekly brief or default tasks must not contain "cto" (exclude tests/, docs that label the strip, README Author if it never had it, and this test file's own assertion strings by checking production modules and html only)
- Scan at least: served HTML, files under app/ that build propose text, any templates under agents/ or similar. Do not fail on STATUS history or this test's name.
- No live network except TestClient.

Verification, exact order. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from fastapi.testclient import TestClient; from app.server import create_app; h=TestClient(create_app()).get('/').text.lower(); print('cto' not in h); assert 'cto' not in h"
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "fix(t141): strip CTO from operator product copy"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

Stop after push. Do nothing else.
