T67 — Quarantine product scaffolds that are not the twin core. Do ONLY this directive. Do not start T68. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend e77fecf. Do not rewrite LICENSE or README Author. Do not weaken T56–T66. English only.

Baseline on main: T01–T66. HEAD e77fecf. pytest was 479 green after T66.

GOAL: marketplace, fake sandbox, pay/auto-pay, omnichannel send, desktop_engine, and KG stubs cannot be mistaken for live product paths.

Implement:

1) For each existing module among core/capsule_marketplace.py, core/marketplace_sync.py, core/runtime_sandbox.py, core/sandbox.py, core/omnichannel.py, core/desktop_engine.py and any kg stub:
- Add module constant QUARANTINED = True
- Public send/pay/charge functions must raise RuntimeError("quarantined") or no-op with that label
- Do not add Stripe or live SMTP

2) tests/test_t67_quarantine.py
- Import those modules when present
- Assert QUARANTINED is True
- Assert a send/pay-like call cannot succeed as live
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "import core.omnichannel as o; print(getattr(o,'QUARANTINED',False))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 479
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t67): quarantine non-core product scaffolds"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
