T41 — Durable commitment scheduler. Do ONLY this directive. Do not start T42. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend f40edaf. Do not rewrite README. Do not send email or open sockets. Source, comments, commit message: English only.

Baseline on main: T01–T40. HEAD f40edaf. pytest was 346 green after T40.

GOAL: a Monday-10:00 commitment survives process restart. Tick only marks due rows; it does not execute side effects.

Implement:

1) core/twin_scheduler.py plus tables in core/twin_persist.py
- jobs(id TEXT PK, tenant_id TEXT, title TEXT, due_at TEXT ISO-8601, timezone TEXT, status TEXT, created_at TEXT)
- schedule(tenant_id, title, due_at, timezone="UTC") -> {id, tenant_id, due_at, status="scheduled"}
- Require consented profile or raise ValueError("no consented profile")
- list_jobs(tenant_id, status=None)
- tick(now=None) -> list of jobs whose due_at <= now and status==scheduled; set those to status="due"
- Do not call email-send, tools, or HTTP

2) API POST /api/v1/twin/schedule body {tenant_id, title, due_at, timezone?}
   POST /api/v1/twin/schedule/tick body {now?}
3) CLI schedule --tenant --title --due
   CLI schedule-tick [--now]

4) tests/test_t41_twin_scheduler.py
- AEGIS_DATA_DIR temp
- without profile ValueError
- schedule then new connection list_jobs still sees the row
- tick with now after due_at flips status to due
- FastAPI 200
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_scheduler import schedule, tick; print(callable(schedule))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 346
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t41): durable commitment scheduler in sqlite"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
