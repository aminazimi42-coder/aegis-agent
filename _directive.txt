T77 — Local data root is an absolute disk path. Do ONLY this directive. Do not start T78. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 7632641. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not weaken T56–T76. English only.

Baseline on main: T01–T76. HEAD 7632641. pytest was 520 green after T76.

GOAL: the desktop view can ask for one absolute data root on the operator disk.

Implement:

1) core/twin_local_view.py
- data_root() -> Path
- Resolve AEGIS_DATA_DIR or default data/ to an absolute path
- mkdir parents exist_ok
- Return that Path

2) tests/test_t77_data_root.py
- AEGIS_DATA_DIR temp
- test_data_root_is_absolute
- test_data_root_creates_dir
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_local_view import data_root; print(data_root().is_absolute())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 520
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t77): local absolute data root"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
