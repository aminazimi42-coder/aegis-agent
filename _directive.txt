T138 — Last slice of this pack: sole local start path, last reject reason on the next propose card. This is 3 of 3 remaining slices. After this file, 0 slices remain in the approved extras-and-depth pack. Do ONLY this directive. Do not start T139. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 01fd0ac. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, SMTP, speech, or a live paid LLM. Do not bind 0.0.0.0. Do not let any specialist execute. Do not weaken T56–T137. English only in repo files.

Baseline on main: T01–T137. HEAD 01fd0ac404d3cac90e9459efbce36d6dd39fdd1a. pytest was 769 passed, 1 skipped after T137.

GOAL: Two depth items only.
1) One documented local start path. Prefer the existing scripts/start_operator.sh or scripts/run_local.sh. Do not invent a second engine launcher. Do not put a new product tree under the git root.
2) The next propose card for that tenant shows the last reject reason when one exists. T130 already persists notes. Surface the latest reason on the proposal text or a typed field. Do not auto-execute.

Implement:

A) Sole start path
- Keep one wrapper script that exports AEGIS_DATA_DIR, uses the project venv, and starts create_app on 127.0.0.1:8741.
- If two wrappers exist, make one call the other or delete the duplicate launcher body. Do not delete a script that tests already import if you can alias it.
- Operator page or README Now: one sentence naming that script. No second start story.
- Do not bind 0.0.0.0. Do not add launchd or a notarized .app.

B) Last reject reason on next card
- On propose, if the tenant has a stored reject note, include it in the new proposal body or a dedicated last_reject_reason field.
- If no reject exists, omit the field. Do not invent a reason.
- Approve notes do not replace the reject reason.
- Specialists still propose only.

C) tests/test_t138_start_and_reject.py
- test_single_start_script_binds_localhost
- test_start_script_sets_data_dir
- test_next_propose_includes_last_reject_reason
- test_no_reject_omits_reason
- No live network except TestClient.

D) README
- At most two honest Now sentences: one start script; last reject reason feeds the next card.
- Do not rewrite Author. Do not claim GA.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('tests/test_t138_start_and_reject.py').read_text().lower(); print(('start' in t) and ('reject' in t) and ('127.0.0.1' in t or 'localhost' in t))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 769
5. git add -A (do not force-add .kms/ or data/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t138): sole start path and last reject reason on next card"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
