T75 — Local home viewer, no network. Do ONLY this directive. Do not start T76. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 84ead64. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not weaken T56–T74. English only.

Baseline on main: T01–T74. HEAD 84ead64. pytest was 513 green after T74.

GOAL: the operator can read home.md from AEGIS_DATA_DIR on disk without calling HTTP.

Implement:

1) core/twin_local_view.py
- read_home(tenant_id: str) -> str
- Reads work_products/{tenant_id}/home.md via existing helpers
- If missing, call render_home(tenant_id) then read
- No urllib, no requests, no sockets

2) tests/test_t75_local_home_view.py
- AEGIS_DATA_DIR temp
- test_read_home_returns_markdown
- test_module_has_no_http_imports
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_local_view import read_home; print(callable(read_home))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 513
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t75): local home viewer without http"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
