T74 — Operator sees why an action stopped. Do ONLY this directive. Do not start T75. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 15d2501. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, desktop UI, or a license server. Do not weaken T56–T73. English only.

Baseline on main: T01–T73. HEAD 15d2501. pytest was 510 green after T73.

GOAL: rejected actions show their stored why_text on home.md so the operator knows why the gate stopped.

Implement:

1) core/twin_home.py
- Add a Rejected group sourced from twin_actions only
- Each rejected row includes why_text from replay_why or the action row
- Empty why renders as ""

2) tests/test_t74_home_why.py
- AEGIS_DATA_DIR temp
- test_home_shows_reject_why
- test_empty_why_is_allowed
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_actions import replay_why; from core.twin_home import render_home; print(callable(replay_why) and callable(render_home))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 510
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t74): show reject why on home"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
