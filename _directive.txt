T151 — Optional license-status client outside execute. This is Layer 3 instruction 3 of 4. Do ONLY this directive. Do not start T152. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, a card form, Grok bots, voice, Windows installer, or a seventh agent. Do not bind 0.0.0.0. Do not weaken T56–T150. English only in repo files. Do not copy $HOME/.aegis. Do not write the word CTO into operator HTML or specialist templates. Do not claim a live paid shop.

Baseline: T150 live e333e5a4658541a2e021c59fcbc8db0df14ad678. pytest 831 passed 1 skipped. Live entitlement is only $AEGIS_DATA_DIR/entitlement.json. Missing or expired file is Echo-limited. Pack ships entitlement.example.json. No HTTP license client exists. Payment stays outside the twin core.

GOAL: One optional labeled HTTP client can ask a separate license host for status. Default is off. Execute, approve, and specialist propose do not call that host. If the URL is unset, behavior stays T146–T148. If the URL is set and the host fails, the page stays Echo-limited and says the check was unreachable. No card data ever leaves the laptop.

Implement:

1) app/licensing/status_client.py (or the existing entitlement module if one already exists — extend, do not fork a second source of truth)
- Function check_remote_status() reads AEGIS_LICENSE_STATUS_URL
- If unset or empty: return a typed local-only result; do not open a socket
- If set: HTTP GET only, timeout <= 2 seconds, no request body, no cookies, no authorization header unless AEGIS_LICENSE_BEARER is set
- Parse only {tier, expires_at, status}; ignore unknown fields
- On timeout, DNS fail, TLS fail, or non-200: return unreachable, never raise into execute
- Never POST. Never send entitlement.json. Never send profile fields.

2) Wire the client only to the existing entitlement read used by the operator page / status payload
- Propose and execute paths must not import or call the HTTP client
- Local file still wins when present and unexpired
- Remote result may only confirm or leave Echo-limited; it must not unlock a tier when the local file is missing

3) Operator page / status JSON
- One English field: license_check = local_only | remote_ok | remote_unreachable | echo_limited
- Do not print the raw URL on the operator HTML
- Do not mention Stripe

4) README Now
- One sentence: optional AEGIS_LICENSE_STATUS_URL is a labeled GET; default off; missing local file stays Echo-limited; no payments in core.
- Do not claim a license server is deployed.

5) tests/test_t151_license_status_client.py
- test_unset_url_does_not_open_socket
- test_unreachable_url_returns_echo_limited_or_unreachable
- test_propose_and_execute_modules_do_not_import_status_client
- test_remote_ok_cannot_unlock_without_local_file
- test_readme_names_optional_license_url
- Mock HTTP. Do not start uvicorn as a subprocess. Do not hit a public host.

Verification, exact order. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; import ast,sys; p=Path('app'); hits=[]
for f in p.rglob('*.py'):
    t=f.read_text(encoding='utf-8')
    if 'AEGIS_LICENSE_STATUS_URL' in t and ('execute' in f.as_posix() or 'propose' in f.as_posix()):
        hits.append(str(f))
print(Path('app/licensing/status_client.py').is_file() or 'AEGIS_LICENSE_STATUS_URL' in Path('app').joinpath('entitlement.py').read_text() if Path('app/entitlement.py').is_file() else Path('app/licensing/status_client.py').is_file(), not hits)"
   Required first token True, second token True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t151): optional license status GET outside execute"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

Stop after push. Do nothing else.
