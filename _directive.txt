T26 — Expense notes from local receipt texts. Do ONLY this directive. Do not start T27. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 6defb0d. Do not rewrite README. Do not send payments or call a bank. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T25. HEAD 6defb0d. pytest was 252 green after T25.

GOAL: turn a folder of receipt .txt files into an expense note. No money movement.

Implement:

1) core/twin_expenses.py
- ingest_receipts(tenant_id, receipts_dir) -> dict {tenant_id, path, count}
- Require consented profile or raise ValueError("no consented profile")
- Require Path(receipts_dir).is_dir() or raise ValueError("receipts dir not found")
- Read each *.txt; first line vendor, rest body
- If a token matches $123 or 123.45 capture as amount else "unparsed"
- Write work_products/{tenant_id}/expenses.md with vendor, amount, and line "Do not pay without written approval."
- ingest_event source="finance" kind="receipt" if allowed; else skip ingest and still write file
- Overwrite on each call

2) API POST /api/v1/twin/expenses/ingest body {tenant_id, receipts_dir}
3) CLI expenses --tenant ID --dir PATH

4) tests/test_t26_twin_expenses.py
- AEGIS_DATA_DIR temp
- without profile ValueError
- after T03 commit write receipt "Hotel\\n$210.00": file contains Hotel and 210 and Do not pay
- FastAPI 200
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_expenses import ingest_receipts; print(callable(ingest_receipts))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 252
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t26): local receipt expense notes without payment"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
