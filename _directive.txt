T116 — LLM provider contract; Echo remains default. Do ONLY this directive. Do not start T117. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 2488e4d. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, SMTP, Gmail, Slack, webhooks, or Postgres. Do not bind 0.0.0.0. Do not let any specialist execute. Do not weaken T56–T115. English only in repo files.

Baseline on main: T01–T115. HEAD 2488e4d8f2de488888e93d7da8a19dee62ead157. pytest was 676 passed, 1 skipped after T115.

GOAL: One formal provider contract. Default is Echo. If the optional HTTP adapter is off, missing, 401, 500, or timed out, complete_safe uses Echo and labels the fallback. CI never needs a paid key.

Implement:

1) Keep existing Echo default and complete_safe gateway. Do not invent a second gateway.

2) If core/llm_provider.py or an equivalent Protocol already exists, extend it. Otherwise add the smallest Protocol:
- name
- is_available() -> bool
- complete(prompt: str) -> str
EchoProvider always is_available True.
HttpProvider is_available False when flag off, base URL missing, or last probe failed.

3) Router rule, hard:
- If HTTP flag is not explicitly on, use Echo.
- If HTTP is on but is_available is False or complete raises timeout/HTTP error, use Echo and tag the result provider=echo(fallback).
- Never switch the process default away from Echo.

4) tests/test_t116_provider_fallback.py
- test_default_is_echo
- test_http_401_falls_back_to_echo
- test_http_timeout_falls_back_to_echo
- test_http_500_falls_back_to_echo
- test_fallback_labeled
- No live network except TestClient. Use mocks only.

5) Do not change README Author. Do not claim a live paid LLM is shipped.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('tests/test_t116_provider_fallback.py').read_text(); print('echo' in t.lower() and 'fallback' in t.lower() and '401' in t)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 676
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t116): echo-default provider fallback contract"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
