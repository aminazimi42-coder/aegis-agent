T51 — Golden busy-day path. Do ONLY this directive. Do not start T52. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 122028c. Do not rewrite README. Do not open SMTP. Source, comments, commit message: English only.

Baseline on main: T01–T50. HEAD 122028c. pytest was 414 green after T50.

GOAL: one test walks event → brief → propose → approve → local outbox. No new product column.

Implement:

1) tests/test_t51_golden_busy_day.py
- AEGIS_DATA_DIR temp
- T03 start/answer/commit for tenant t51
- ingest one calendar or git event if those helpers exist; otherwise skip ingest
- render_brief writes morning_brief.md
- propose_actions returns at least one action
- pick one action, set title so classify is L2 (contains "email"), approve it
- give payload {"body": "Busy day pack"} if the store accepts payload
- send_approved writes an .eml under work_products/t51/outbox
- assert .eml exists and contains "Busy day pack" or the title
- FastAPI health or home 200 optional
- No live network

2) Do not add new core modules unless a one-line helper is required to set payload on an existing action.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; print(Path('tests/test_t51_golden_busy_day.py').is_file())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 414
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "test(t51): golden busy-day brief propose approve outbox"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
