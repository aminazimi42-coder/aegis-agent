T104 — First macOS app bundle and installer. Do ONLY this directive. Do not start T105. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 00ba7c4. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not revive the quarantined desktop_engine.py as the product. Do not add Electron. Do not weaken T56–T103. English only.

Baseline on main: T01–T103. HEAD 00ba7c4. pytest was 631 green after T103.

GOAL: a real double-clickable macOS .app that launches the local twin. No window UI yet beyond a stub page. Data stays under ~/.aegis.

Implement:

1) desktop/macos/Aegis.app layout
- Contents/Info.plist
- Contents/MacOS/Aegis launcher script
- Contents/Resources/stub.html that says Aegis local twin, not SaaS

2) launcher
- starts the existing local HTTP app on 127.0.0.1 only
- opens stub.html or the local URL with the default macOS opener
- does not bind 0.0.0.0
- does not read repo data_root as the live store; AEGIS_DATA_DIR defaults to $HOME/.aegis

3) scripts/install_macos_app.sh
- copies desktop/macos/Aegis.app to $HOME/Applications/Aegis.app
- does not write into the git worktree as the installed app
- prints the install path

4) tests/test_t104_macos_app.py
- test_bundle_has_plist_and_launcher
- test_install_script_exists
- test_stub_html_exists
- skip live window
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; print((Path('desktop/macos/Aegis.app/Contents/Info.plist').is_file()) and Path('scripts/install_macos_app.sh').is_file())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 631
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t104): first macOS app bundle and installer"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
