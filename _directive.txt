T81 — HTTP path consumes quota; Echo does not. Do ONLY this directive. Do not start T82. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend c95d8b9. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not weaken T56–T80. English only.

Baseline on main: T01–T80. HEAD c95d8b9. pytest was 536 green after T80.

GOAL: one successful HTTP complete decrements remaining by 1. Echo complete leaves remaining unchanged.

Implement:

1) core/twin_quota.py
- consume_quota(tenant_id: str, n: int = 1) -> int
- Decrements remaining, never below 0
- Returns the new remaining

2) core/llm_safety.py complete_safe()
- After a successful provider call, if provider_kind is http-fallback, call consume_quota(tenant_id, 1)
- If provider_kind is echo, do not consume
- If consume fails, still return the completion

3) tests/test_t81_http_consumes_quota.py
- AEGIS_DATA_DIR temp
- test_http_complete_decrements_quota
- test_echo_complete_does_not_decrement
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_quota import consume_quota; print(callable(consume_quota))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 536
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t81): http path consumes quota echo does not"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
