T160 — Confidence on propose card. This is slice 2 of 4. Do ONLY this directive. Do not start T161. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, a card form, a live license host, Grok bots, voice, Windows installer, WebSockets, Playwright, OS cron, or a seventh agent. Do not bind 0.0.0.0. Do not weaken T56–T159. English only in repo files. Do not copy $HOME/.aegis. Do not write the word CTO into operator HTML or specialist templates.

Baseline: T159 live 41e45b6f3045cf27b89472e93f0d90437d4525a6. pytest 875 passed 1 skipped.

GOAL: Every propose card carries a deterministic confidence integer 0–100. Below threshold the card asks a clarifying question and stays proposed. Confidence never executes, never bypasses Approve, never changes the digest bind.

Implement:

1) Confidence field
- Add confidence: int on the propose payload shown on the operator card.
- Compute it locally from existing signals only: specialist match to the ask, presence of stored profile fields used in the body, and RISK_L*. No new model. Echo path included.
- Threshold default 40. If confidence < 40, the card body must include one short English clarifying question. It must not invent an answer. It must not execute.

2) Operator page
- Show the integer on the card next to the specialist name. Example: Confidence 72.
- Do not add a new column. Do not add a SaaS meter. Do not auto-approve high scores.

3) README Now — one sentence only:
- Propose cards show a local 0–100 confidence integer; scores below 40 ask a clarifying question and still require Approve.

4) tests/test_t160_propose_confidence.py
- test_propose_payload_includes_confidence_0_100
- test_low_confidence_includes_clarifying_question
- test_low_confidence_does_not_execute
- test_high_confidence_still_requires_approve
- test_readme_mentions_confidence
- Do not start uvicorn. Use tmp_path. Do not write the developer's real $HOME/.aegis.

Verification, exact order. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; r=Path('README.md').read_text(); print('confidence' in r.lower(), Path('tests/test_t160_propose_confidence.py').is_file())"
   Required: True True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t160): local propose confidence with ask-not-execute"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

Stop after push. Do nothing else.
