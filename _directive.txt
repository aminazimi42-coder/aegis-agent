T37 — GitHub Actions CI on main. Do ONLY this directive. Do not start T38. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend d9f2950. Do not rewrite README again. Do not add paid SaaS secrets. Source and commit message: English only.

Baseline on main: T01–T36. HEAD d9f2950. pytest was 325 green after T36.

GOAL: one workflow that on push and pull_request to main runs ruff then pytest on Python 3.11.

Implement:

1) .github/workflows/ci.yml
- name: CI
- on push and pull_request branches [main]
- job test, runs-on ubuntu-latest
- checkout@v4
- setup-python@v5 with python-version 3.11
- pip install -e ".[dev]"
- python -m ruff check .
- python -m pytest -q
- no network calls in the job besides pip and GitHub

2) Do not delete existing release.yml or security.yml

3) tests/test_t37_ci_workflow.py
- Path(".github/workflows/ci.yml").is_file()
- text contains "ruff check" and "pytest"
- text contains "3.11"
- text contains "pull_request"
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('.github/workflows/ci.yml').read_text(); print('pytest' in t and '3.11' in t)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 325
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "ci(t37): GitHub Actions ruff and pytest on main"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
