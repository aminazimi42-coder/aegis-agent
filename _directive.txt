T133 — Honest README now vs destination. Do ONLY this directive. Do not start T134. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 6013db4. Do not rewrite LICENSE. Keep the README Author paragraph byte-stable. Do not add Stripe, Grok bots, SMTP, Gmail, Slack, webhooks, Postgres, speech-to-text, or a live paid LLM. Do not bind 0.0.0.0. Do not let any specialist execute. Do not weaken T56–T132. English only in repo files.

Baseline on main: T01–T132. HEAD 6013db4. pytest was 744 passed, 1 skipped after T132.

GOAL: README states this is a local digital-twin operator and splits Now from Destination. Destination is labeled planned. Now matches tests.

Implement:

1) README.md
- Keep Author paragraph unchanged
- Add a short Now section: six specialists propose only, hash approve, local execute, Echo default, durable profile, approve/reject notes, local entitlement file, start_operator.sh plus .app wrapper. Not a store installer. Not a cloud brain.
- Add a Destination section labeled Planned: double-click installer, local model on the existing adapter, weekly-brief button, multi-week style loop, license server outside core, Professional then Executive and Engineering. Payments never enter core.
- Do not claim three CTO trials already ran
- Do not claim Grok Bot is Aegis

2) STATUS.md one line pointing at those two README sections

3) tests/test_t133_readme_roadmap.py
- test_author_paragraph_unchanged
- test_readme_has_now_and_planned
- test_readme_does_not_claim_store_installer
- No live network except TestClient

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('README.md').read_text().lower(); print('planned' in t and 'echo' in t)"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 744
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "docs(t133): readme now vs planned destination"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
