T103 — Structured truthful README. Do ONLY this directive. Do not start T104. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 188c058. Do not rewrite LICENSE. Do not rewrite the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not add Electron. Do not weaken T56–T102. English only.

Baseline on main: T01–T102. HEAD 188c058. pytest was 631 green after T102.

GOAL: README.md becomes the operator front door. Pretty structure, true claims only.

Keep the existing Author block byte-for-byte.

Required structure:
- short title + one-line what it is: local-first cognitive twin CLI
- mermaid of interview → propose → digest-approve → execute → receipt
- table Shipped / Not shipped matching STATUS.md
- table of real `aegis` commands that exist today
- six specialists propose only
- Echo default, HTTP optional, email outbox only
- data lives under the user data root
- desktop_engine.py and .market are quarantined leftovers
- no production-SaaS claim, no fake coverage percent, no LangGraph

Use GitHub markdown: headings, tables, one mermaid block, fenced command examples. No GIF spam. No invented badges.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('README.md').read_text(); print(('mermaid' in t) and ('aegis propose' in t) and ('Author' in t))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count >= 631
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "docs(t103): structured truthful README"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
