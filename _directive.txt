T46 — Restart contract tests for persist, spend, jobs. Do ONLY this directive. Do not start T47. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 5c61c61. Do not rewrite README. Source, comments, commit message: English only.

Baseline on main: T01–T45. HEAD 5c61c61. pytest was 393 green after T45.

GOAL: one test module proves the durable pieces survive a second process. No new product feature.

Implement:

1) tests/test_t46_restart_contract.py only (plus tiny helper in core/twin_persist.py only if a function is missing)
- AEGIS_DATA_DIR temp shared across two subprocesses
- Subprocess A: put_approval(... status pending), add_spend(tenant, 10), schedule a job due in the past after T03 commit in that process
- Subprocess B: get_approval finds the row; get_budget spent >= 10; list_jobs sees the job
- Tick in B flips due if still scheduled
- No live network
- Do not add desktop, payments, or README claims

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; print(Path('tests/test_t46_restart_contract.py').is_file())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 393
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "test(t46): restart contract for approvals budgets and jobs"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
