T145 — Pack start at folder root + honest copy path. This is pack follow-up 1 of 1. Do ONLY this directive. Do not start T146. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not copy $HOME/.aegis. Do not add Stripe, Grok bots, voice, Windows installer, or a seventh agent. Do not bind 0.0.0.0. Do not weaken T56–T144. English only in repo files.

Baseline: T144 live e28fefec. pytest 805 passed 1 skipped. First Mac user account amin proved the loop, but had to run scripts/start_operator.sh and could not write into /Users/Shared.

GOAL: A copied pack folder starts with ./start_operator.sh at the pack root. INSTALL.md tells the operator to copy the folder into that account $HOME, not into /Users/Shared. Data dir stays $HOME/.aegis. Script still does not open Safari.

Implement:

1) scripts/pack_local_operator.sh
- Keep T143/T144 exclusions.
- After packing scripts/start_operator.sh, also write an executable start_operator.sh at dist/aegis-local-operator/start_operator.sh.
- Root start_operator.sh may be a thin wrapper that execs scripts/start_operator.sh, or a copy. Same env: AEGIS_DATA_DIR default $HOME/.aegis, bind 127.0.0.1:8741, do not open a browser.
- Second pack run replaces dist/aegis-local-operator/, does not append.

2) dist pack INSTALL.md (generated by the pack script)
- Now steps only: need Python 3.11; copy this folder into the operator $HOME (example $HOME/aegis-local-operator); do not use /Users/Shared unless that account can write there; python3.11 -m venv .venv; pip install -e .; ./start_operator.sh; open http://127.0.0.1:8741/ yourself.
- Say data lives in that account $HOME/.aegis.
- Do not claim notarized .app, Windows, or friend-ready App Store install.

3) README Now
- One sentence: packed folder starts with ./start_operator.sh at pack root; copy the folder into the operator home, not Shared, if Shared is not writable.
- Do not claim the Shared copy path was proven.

4) tests/test_t145_pack_root_start.py
- test_packed_root_start_script_exists: after running pack script, dist/aegis-local-operator/start_operator.sh is a file
- test_root_start_exports_data_dir_and_port: root start script text contains AEGIS_DATA_DIR and 127.0.0.1 and 8741
- test_install_md_says_home_not_shared: INSTALL.md mentions $HOME or home and does not require /Users/Shared
- test_pack_still_omits_builder_files: no .git, no _directive.txt, no task_instruction.txt in packed tree
- Do not start uvicorn in the test.

Verification, exact order. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   bash scripts/pack_local_operator.sh >/tmp/t145-pack.out
   ./.venv/bin/python -c "from pathlib import Path; r=Path('dist/aegis-local-operator'); print(r.is_dir(), (r/'start_operator.sh').is_file(), (r/'scripts/start_operator.sh').is_file(), (r/'_directive.txt').exists())"
   Required: True True True False
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "fix(t145): pack root start script and home copy path"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

Stop after push. Do nothing else.
