T76 — Local queue view, no network. Do ONLY this directive. Do not start T77. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 0429cfd. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not weaken T56–T75. English only.

Baseline on main: T01–T75. HEAD 0429cfd. pytest was 516 green after T75.

GOAL: the operator can list pending and approved-waiting actions from disk/SQLite without HTTP.

Implement:

1) core/twin_local_view.py
- list_queue(tenant_id: str) -> dict with keys pending and approved_waiting
- Source twin_actions only
- No urllib, no requests, no sockets

2) tests/test_t76_local_queue_view.py
- AEGIS_DATA_DIR temp
- test_list_queue_splits_pending_and_approved
- test_list_queue_excludes_executed
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_local_view import list_queue; print(callable(list_queue))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 516
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t76): local queue view without http"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
