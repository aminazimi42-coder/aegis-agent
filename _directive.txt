T06 — Twin proposed actions with human approval. Do ONLY this directive. Do not start T07. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 75759ed. Do not rewrite README. Do not add payments, bank transfer, email send, GitHub OAuth, Telegram, or desktop UI. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T05. pytest was 133 green after T05.

GOAL: the twin may PROPOSE work items from the profile + digest, but it must not act until the human approves. Execution for this slice is a stub that records "executed" in SQLite only. No external side effects.

Implement:

1) core/twin_actions.py
- Action kinds allowlist: "review_digest", "review_repos", "prepare_weekly_plan"
- propose_actions(tenant_id) -> list of actions. Require consented profile or raise ValueError("no consented profile")
  Deterministic: one review_digest, plus review_repos if layers.repositories nonempty, plus prepare_weekly_plan
- Each action: action_id, tenant_id, kind, title, status (proposed|approved|rejected|executed), created_at
- Persist table twin_actions via core.persistence
- approve(action_id) sets approved; reject(action_id) sets rejected; unknown id raises ValueError
- execute(action_id) only if status==approved else PermissionError("approval required"); then status=executed and write evidence ledger action="twin_action_executed" if ledger works
- list_actions(tenant_id)

2) API /api/v1/twin
- POST /actions/propose  body {tenant_id}
- POST /actions/{action_id}/approve
- POST /actions/{action_id}/reject
- POST /actions/{action_id}/execute
- GET  /actions/{tenant_id}
Keep T03–T05 routes working. Do not break digest or observe/git.

3) tests/test_t06_twin_actions.py
- propose without profile raises ValueError
- after T03 commit: propose returns >=1 proposed
- execute before approve raises PermissionError
- approve then execute -> status executed
- FastAPI: propose 200, approve 200, execute 200
- AEGIS_DATA_DIR temp isolation; no network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof — write a short /tmp/t06_proof.py if the one-liner is awkward. Required stdout:
   GATED
   executed
   Logic: T03 interview+commit for tenant t6, propose_actions, execute(first_id) must raise PermissionError and print GATED, then approve+execute prints executed.
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 133
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t06): proposed twin actions with human approval gate"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
