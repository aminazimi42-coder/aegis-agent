T55 — Reproducible green baseline. Do ONLY this directive. Do not start T56. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 45483ca. Do not rewrite README or LICENSE. Do not add a cloud KMS. Source, comments, commit message: English only.

Baseline on main: T01–T54. HEAD 45483ca. pytest was 433 green after T54.

GOAL: declared Python 3.11 install plus CI reproduce one green suite. Default key store is kms_adapter. Persistence failure is not swallowed.

Implement:

1) pyproject.toml
- Keep python >=3.11
- Dev extra must include the HTTP test client Starlette actually imports (httpx and, if the resolved Starlette requires it, httpx2)
- Do not drop pytest or ruff

2) .github/workflows/ci.yml
- Stay on Python 3.11
- After install, print: python version, then pytest --collect-only -q | tail
- Then ruff check . and pytest -q
- If uv.lock exists, prefer `uv sync --frozen --extra dev` when uv is available; otherwise keep pip install -e ".[dev]"

3) core/key_manager.py
- Default persist path: kms_adapter only
- Do not auto-select kms_enterprise because it imported
- register_private_key must not swallow store errors; raise a short English error on persist failure
- Isolate any file paths under AEGIS_DATA_DIR when that env is set

4) tests/test_t55_build_contract.py
- AEGIS_DATA_DIR temp
- test_declared_test_client_dependency_imports (import httpx succeeds)
- test_default_key_store_round_trip_under_aegis_data_dir
- test_persistence_failure_is_not_swallowed (if a store hook can be forced to fail)
- Do not xfail. No live network. Do not pre-create .kms files to hide a fail

5) Fix tests/test_key_manager.py only if it still expects enterprise store while default is adapter.

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "import httpx; from core.key_manager import KeyManager; print(hasattr(KeyManager, 'register_private_key'))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 433
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "build(t55): lock reproducible test baseline"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
