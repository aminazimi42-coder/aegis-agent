T12 — Local work-product files from the twin. Do ONLY this directive. Do not start T13. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 7788df1. Do not rewrite README. Do not add OAuth, desktop UI, or payments. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T11. pytest was 177 green after T11.

GOAL: the twin must emit real local artifacts a CTO can open: weekly plan + repo review notes. Files only. No email, no GitHub write.

Implement:

1) core/twin_work_products.py
- render(tenant_id) -> dict {tenant_id, dir, files: list[str]}
- Require consented profile or raise ValueError("no consented profile")
- Call rebuild() from twin_behavior if get_behavior is None (safe if already imported)
- Write under Path(AEGIS_DATA_DIR or ./data)/work_products/{tenant_id}/
  - weekly_plan.md  — title, profile role if any, repos from behavior or layers, event_count, 3 bullet next actions (deterministic text, not a live LLM)
  - review_notes.md — one section per repo name
- Overwrite those two files on each render
- Return absolute or as_posix paths in files

2) API
- POST /api/v1/twin/work-products/render body {tenant_id}
Keep existing routes.

3) tests/test_t12_twin_work_products.py
- AEGIS_DATA_DIR temp
- render without profile raises ValueError
- after T03 commit + ingest git repo acme/core: render creates both md files; weekly_plan.md contains acme/core; files len == 2
- FastAPI client 200
- No live network / no paid LLM required

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_work_products import render; print(callable(render))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 177
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t12): render local weekly plan and review notes"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
