T178_CONTINUE — Finish T178 only. Do not start T179. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, voice, Windows, a seventh agent, or a signed-app claim. Do not write the word notarized into README. English only in repo files. Do not copy $HOME/.aegis.

HARD STOP: write the missing tests, then pytest, then commit, then push in THIS same turn. After push, stop. Do not ask whether to continue.

Already in the tree if the previous turn wrote them: profile schema validate_profile, GET profile returns profile_invalid on bad load, signed_export writes sibling .sig, verify_local_sig returns Intact/Tampered/Missing. Do not rip those out. Complete what is missing.

1) Create tests/test_t178_profile_export_sig.py as valid Python 3.11.
- Every def must have matching parentheses. Compile before pytest.
- test_valid_profile_still_prefills
- test_invalid_profile_typed_error
- test_signed_export_writes_sig_sibling
- test_missing_brief_is_not_success
- test_local_sig_intact
- test_local_sig_tampered
- test_readme_author_untouched
- Use tmp_path. Do not write the live $HOME/.aegis. Do not start uvicorn.

2) Then this exact order. If a proof fails, fix only that failure and restart from 1 in the SAME turn.

1. ./.venv/bin/python -m py_compile tests/test_t178_profile_export_sig.py
2. ./.venv/bin/python -m ruff check --fix .
3. ./.venv/bin/python -m ruff check .
4. Proof:
   ./.venv/bin/python -c "from pathlib import Path; r=Path('README.md').read_text(); print(('schema' in r.lower() or '.sig' in r.lower() or 'signature' in r.lower()), 'Author' in r, 'notarized' not in r.lower())"
   Required: True True True
5. ./.venv/bin/python -m pytest -q
   Required: 0 failed
6. git add -A (do not force-add dist/, .kms/, *.cer, *.p8, *.p12, or $HOME/.aegis/; do not delete _directive.txt)
7. git commit -m "feat(t178): profile schema and detached brief signature"
8. git push origin main
9. Print new SHA, pytest count, and the proof line.

After step 5 you MUST run steps 6–9 in the same turn. Stop only after a successful push. Do nothing else.
