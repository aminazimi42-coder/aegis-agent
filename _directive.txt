T14 — Local ICS calendar ingest. Do ONLY this directive. Do not start T15. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend f3eae63. Do not rewrite README. Do not add Google Calendar OAuth, desktop UI, or payments. Source, comments, commit message: English only.

Baseline on main (do not regress): T01–T13. pytest was 186 green after T13.

GOAL: ingest a local .ics file as calendar events into the twin. No cloud calendar.

Also one-line CLI bootstrap: at top of tools/twin_cli.py, if `core` is not importable, insert the repo root onto sys.path so `python tools/twin_cli.py status` works without extra env.

Implement:

1) core/twin_calendar.py
- ingest_ics(tenant_id, ics_path) -> dict {tenant_id, ingested, skipped}
- Require consented profile or raise ValueError("no consented profile")
- Require Path(ics_path).is_file() or raise ValueError("ics not found")
- Parse VEVENT blocks without an extra library: for each VEVENT read SUMMARY and DTSTART if present
- ingest_event source="calendar" kind="meeting" payload={summary, start} then evolve
- Dedup by (summary, start) already stored
- Ignore malformed events (count as skipped)

2) API
- POST /api/v1/twin/calendar/ics body {tenant_id, ics_path}
Keep existing routes.

3) CLI
- tools/twin_cli.py subcommand: calendar-ics --tenant ID --path FILE
- sys.path bootstrap as above

4) tests/test_t14_twin_calendar.py
- AEGIS_DATA_DIR temp
- without profile ValueError
- write a tiny temp .ics with one VEVENT SUMMARY:Standup DTSTART:20260903T090000Z
- after T03 commit: ingest_ics ingested >= 1; second call ingested == 0
- FastAPI 200
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python tools/twin_cli.py status
   Required: exit 0 and stdout contains 1.0.0-rc1
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 186
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t14): local ICS calendar ingest into twin events"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
