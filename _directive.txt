T150 — App icon and uninstall that leaves local data. This is Layer 3 instruction 2 of 4. Do ONLY this directive. Do not start T151. Do not read task_instruction.txt. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, a live license server, Grok bots, voice, Windows installer, or a seventh agent. Do not bind 0.0.0.0. Do not weaken T56–T149. English only in repo files. Do not copy $HOME/.aegis. Do not write the word CTO into operator HTML or specialist templates. Do not claim notarization, App Store, or Developer ID.

Baseline: T149 live 338f84468d28be3e7014e32b0479282c80263d4d. pytest 826 passed 1 skipped. dist/AegisOperator-mac has Install.command, unsigned Aegis Operator.app, and the packed tree. No icon. No uninstall path. Owner Mac trial of the installer is tomorrow, not this slice.

GOAL: The unsigned app bundle ships a local icon resource. One uninstall script removes the copied operator folder and does not delete $HOME/.aegis. INSTALL tells the operator that profile, actions, and exports stay on that laptop after uninstall.

Implement:

1) scripts/build_mac_installer.sh
- Keep T149 replace-not-append behavior and exclusions.
- Write dist/AegisOperator-mac/Aegis Operator.app/Contents/Resources/AppIcon.png
- The PNG may be a small generated shield-style mark; do not download a remote image; do not vendor a huge binary.
- Info.plist must name CFBundleIconFile as AppIcon.png or AppIcon
- MacOS stub still sets AEGIS_DATA_DIR default $HOME/.aegis and execs the adjacent pack start_operator.sh
- Still no Safari, no codesign, no notarytool

2) scripts/uninstall_aegis_operator.sh
- Executable.
- Removes only $HOME/aegis-local-operator if present.
- Must not run rm -rf on $HOME/.aegis or any path under it.
- Must not touch /Users/hermesdev/Aegis Agent or /Users/Shared.
- Print one English block: what was removed, that $HOME/.aegis was left in place, and that a later install reuses that data.
- Do not open a browser. Do not kill port 8741.

3) Install.command and generated INSTALL.md
- Name scripts/uninstall_aegis_operator.sh or the copy of that script inside dist/AegisOperator-mac/uninstall.command
- One Now line: uninstall removes the copied program folder; it does not delete $HOME/.aegis
- Keep the unsigned / not App Store sentence from T149

4) README Now
- One sentence: uninstall script leaves $HOME/.aegis; installer app is still unsigned.
- Do not claim the owner already ran uninstall on a stranger account.

5) tests/test_t150_icon_uninstall.py
- test_app_bundle_has_icon_resource
- test_plist_names_icon
- test_uninstall_script_exists_and_executable
- test_uninstall_script_does_not_delete_aegis_data_dir
- test_install_md_says_uninstall_keeps_data
- Do not start uvicorn as a subprocess.
- Do not run uninstall against the builder home.

Verification, exact order. If a proof fails, fix only that failure and restart from 1.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   bash scripts/build_mac_installer.sh >/tmp/t150-build.out
   ./.venv/bin/python -c "from pathlib import Path; r=Path('dist/AegisOperator-mac'); app=r/'Aegis Operator.app'; print((app/'Contents/Resources/AppIcon.png').is_file(), (r/'uninstall.command').is_file() or Path('scripts/uninstall_aegis_operator.sh').is_file(), 'aegis' in Path('scripts/uninstall_aegis_operator.sh').read_text().lower() if Path('scripts/uninstall_aegis_operator.sh').is_file() else False, (r/'_directive.txt').exists())"
   Required: True True True False
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or $HOME/.aegis/; do not delete _directive.txt)
6. git commit -m "feat(t150): installer icon and data-preserving uninstall"
7. git push origin main
8. Print new SHA, pytest count, and the proof line.

Stop after push. Do nothing else.
