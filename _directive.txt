T52 — Local execute after approve. Do ONLY this directive. Do not start T53. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 6556c25. Do not rewrite README. Do not open sockets. Source, comments, commit message: English only.

Baseline on main: T01–T51. HEAD 6556c25. pytest was 417 green after T51.

GOAL: execute() still refuses unapproved actions. After approve, email-kind writes local outbox; other kinds write a local executed.md. No SMTP.

Implement:

1) core/twin_actions.py execute
- Keep PermissionError("approval required") if status is not approved
- After marking executed:
  - if title or kind lower contains "email", call send_approved(tenant_id, action_id) best-effort
  - else write work_products/{tenant_id}/executed.md containing action_id and title
- Do not call HTTP

2) tests/test_t52_local_execute.py
- AEGIS_DATA_DIR temp
- unapproved execute raises PermissionError
- after T03 commit, approve an email-titled action with payload body, execute creates an .eml or executed.md
- FastAPI execute route if present still rejects unapproved
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_actions import execute; print(callable(execute))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 417
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t52): local execute after approve writes outbox or executed.md"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
