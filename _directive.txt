T112 — Distinct specialist proposals, still propose-only. Do ONLY this directive. Do not start T113. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 4ceddbe. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, or a live LLM. Do not bind 0.0.0.0. Do not let any specialist execute. Do not weaken T56–T111. English only.

Baseline on main: T01–T111. HEAD 4ceddbe. pytest was 660 green after T111.

BUG: all six queue rows repeat the operator task text. A CTO cannot tell specialists apart.

GOAL: each specialist propose() returns a distinct, deterministic, role-shaped proposal that still includes the operator text. No network. No execute.

Implement:

1) Distinct proposal bodies
- Bita: insight / what the ask means
- Kian: execution steps that remain unexecuted
- Alina: strategy / priority order
- Aylin: quality / what would make the output fail review
- Ahmad: security / what must not happen
- Amin: finance / cost or quota caution
- keep payload_sha256 over the canonical envelope
- Echo default unchanged

2) tests/test_t112_distinct_proposals.py
- test_six_bodies_not_identical
- test_each_name_in_own_row
- test_still_propose_only
- No live network except TestClient

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; p=Path('core'); print(p.exists())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 660
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t112): distinct specialist proposals"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
