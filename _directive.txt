T115 — Truth surfaces, local privacy model, SQLite durability proof. Do ONLY this directive. Do not start T116. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend HEAD. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe, Grok bots, a live paid LLM, SMTP, Gmail, Slack, webhooks, or Postgres. Do not bind 0.0.0.0. Do not let any specialist execute. Do not weaken T56–T114. English only in repo files.

Baseline on main: T01–T114 plus later shipped local-operator slices. Work on current origin/main HEAD.

GOAL: Documents and one local proof match the tree. Aegis is a local-first operator twin with six specialists, Echo default, hash-bound approve, SQLite on disk. No production-SaaS claim. No card in core.

Implement:

1) docs/PRIVACY_MODEL.md (new, short, honest)
- Data lives in AEGIS_DATA_DIR on the operator machine
- Default inference is local Echo; a labeled HTTP adapter is optional and off unless configured
- Approve requires tenant+actor+digest; specialists propose only
- No payment processor in twin core
- Grok Bot desktop teammates are a separate cloud product, not this core
- Do not claim end-to-end cloud encryption or hosted multi-tenant SaaS

2) STATUS.md
- Add a "Privacy and durability" line that points at docs/PRIVACY_MODEL.md
- State SQLite local profile+actions+jobs+receipts
- Do not add Gmail/Slack/SMTP as shipped
- Keep six names: Bita Kian Alina Aylin Ahmad Amin

3) README.md
- If any sentence still claims hosted billing, live paid LLM default, or four-agent-only product, fix that sentence only
- Keep the existing Author paragraph byte-for-byte
- Keep both brand strings: "AI Architect Amin Azimi — End-to-End System Development" and "Azimi Innovation Lab"

4) Durability proof helper
- If scripts/backup_local.sh or the T69 backup path exists, do not replace it
- Add tests/test_t115_privacy_durability.py only
- test_privacy_model_file_exists
- test_privacy_model_forbids_psp_in_core
- test_status_names_six_agents
- test_readme_author_brands_intact
- test_sqlite_profile_survives_reopen: write a profile via the existing API or store, close the client, open a new client against the same AEGIS_DATA_DIR, read it back
- No live network except TestClient

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; p=Path('docs/PRIVACY_MODEL.md').read_text(); print('AEGIS_DATA_DIR' in p and 'Echo' in p and 'Stripe' not in p and 'payment' in p.lower())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "docs(t115): privacy model and local durability proof"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
