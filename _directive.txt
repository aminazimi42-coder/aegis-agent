T36 — README from the real repo. Do ONLY this directive. Do not start T37. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 1594428. Do not add payments or live LLM. Source and commit message: English only.

Baseline on main: T01–T35. HEAD 1594428. pytest was 320 green after T35.

GOAL: rewrite README.md so it matches files that exist today. No feature that is not in this tree.

Implement:

1) Inventory first (read-only):
- tools/twin_cli.py subparsers
- @app.post/@app.get paths under /api/v1/twin in app/server.py
- core/twin_*.py module names
- pytest count from running pytest -q is allowed
- Keep existing hero svg if docs/assets/aegis-hero.svg exists

2) Rewrite README.md in English, short:
- Title + one sentence of what the tree actually is (FastAPI + local twin work products + human approve gate)
- Quick start: venv, pytest, uvicorn if those files exist
- Table of twin CLI commands that are in twin_cli.py (not invented)
- Table of twin POST routes that are in server.py (not invented)
- State EchoProvider / no paid LLM unless you find a real paid adapter wired as default
- State SQLite under AEGIS_DATA_DIR
- State free Render may cold-start
- Do not claim a desktop UI unless you find one
- Do not delete LICENSE badge if LICENSE exists

3) tests/test_t36_readme.py
- README.md exists
- contains "EchoProvider" or "no paid LLM"
- contains "brief-morning" and "email-send" and "audio-task"
- contains "/api/v1/twin/resume/render"
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; t=Path('README.md').read_text(); print('brief-morning' in t and 'EchoProvider' in t or 'no paid LLM' in t.lower())"
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 320
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "docs(t36): README matches twin CLI routes and local constraints"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
