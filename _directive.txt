T84 — Local CLI shows home, queue, and provider status. Do ONLY this directive. Do not start T85. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 5a17bf0. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not un-quarantine desktop_engine.py. Do not add Electron. Do not weaken T56–T83. English only.

Baseline on main: T01–T83. HEAD 5a17bf0. pytest was 545 green after T83.

GOAL: python -m core.twin_local prints home markdown, pending vs approved-waiting counts, and provider_status. No HTTP.

Implement:

1) core/twin_local.py
- main(argv: list[str] | None = None) -> int
- Default command: status
- Uses read_home, list_queue, provider_status, data_root from twin_local_view
- Prints sections: HOME, QUEUE, PROVIDER
- Exit 0
- No urllib, requests, socket

2) core/__main__ is NOT required. Entry is python -m core.twin_local

3) tests/test_t84_local_cli.py
- AEGIS_DATA_DIR temp
- test_main_prints_home_and_queue
- test_main_prints_provider_kind
- test_module_has_no_http_imports
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from core.twin_local import main; print(callable(main))"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 545
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t84): local cli home queue provider status"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
