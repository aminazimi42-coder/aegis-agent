T108 — macOS app icon from local cover file. Do ONLY this directive. Do not start T109. Do not read task_instruction.txt. Do not patch SKILL.md or files outside this repo. Do not force-push or amend 26af939. Do not rewrite LICENSE or the README Author paragraph. Do not add Stripe or a license server. Do not add Electron. Do not bind 0.0.0.0. Do not weaken T56–T107. English only.

Baseline on main: T01–T107. HEAD 26af939. pytest was 646 green after T107.

GOAL: Aegis.app shows a real icon, not the generic document tile.

Implement:

1) Icon source
- if $HOME/Downloads/aegis-cover.png exists, copy it to desktop/macos/Aegis.app/Contents/Resources/AppIcon.png
- else write a 1024x1024 navy/gold PNG to that same path (Pillow or stdlib is fine)
- do not download from the internet

2) Info.plist
- CFBundleIconFile = AppIcon
- keep existing bundle id

3) If iconutil exists, build AppIcon.icns next to the png. If not, png-only is acceptable.

4) tests/test_t108_app_icon.py
- test_appicon_png_exists
- test_plist_names_appicon
- No live network

Verification, exact order. If a proof fails, fix only that failure and restart from 1. Do not commit a red suite.

1. ./.venv/bin/python -m ruff check --fix .
2. ./.venv/bin/python -m ruff check .
3. Proof:
   ./.venv/bin/python -c "from pathlib import Path; print(Path('desktop/macos/Aegis.app/Contents/Resources/AppIcon.png').is_file())"
   Required: True
4. ./.venv/bin/python -m pytest -q
   Required: 0 failed, count > 646
5. git add -A (do not force-add .kms/ or data/; do not delete _directive.txt)
6. git commit -m "feat(t108): macOS app icon"
7. git push origin main
8. Print new SHA, pytest count, proof output.

Stop after push. Do nothing else.
